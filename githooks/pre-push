#!/usr/bin/env sh
set -eu

while read local_ref local_sha remote_ref remote_sha
do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "[pre-push] Direct push to main is blocked. Open a PR."
    exit 1
  fi
done

echo "[pre-push] Running full verification..."
npm run verify
