# Task Card

- ID: `TASK-0208`
- Scope: Claude memory infrastructure — step 2/3: automatic, git-persistent session journal. Every commit auto-appends a memory line that travels inside the commit; SessionStart reads it back. Closes the write→read memory loop ("otomatik güncellensin").
- Affected files:
  - `scripts/auto-journal.mjs` (new — git pre-commit step, appends + stages journal line)
  - `githooks/pre-commit` (calls auto-journal after verify:staged)
  - `scripts/session-brain.mjs` (now surfaces last 8 journal entries at SessionStart)
  - `docs/SESSION-JOURNAL.md` (auto-created, append-only)
  - `docs/CHANGELOG.md`
- Do-not-touch list: all PROTECTED files, `.env*`
- Done criteria: a commit creates/updates `docs/SESSION-JOURNAL.md` and includes it in the same commit (clean tree after); `session-brain.mjs` output shows the journal tail.
- Rollback steps: revert `githooks/pre-commit`, re-run `npm run hooks:install`; delete `scripts/auto-journal.mjs`.
- Owner: Doğan / Claude
- Date: 2026-06-07

## Notes
- Chosen git pre-commit (not Claude Stop hook) for per-commit granularity + git persistence across ephemeral containers, with no dirty-state side effects.
