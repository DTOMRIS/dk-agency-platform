# TASK-0412 — toolkit number formatting (delivery-calc, menu-matrix, yemek-xerci, menyu-analitigi)

- ID: `TASK-0412`
- Status: ✅ Tamamlandı (kod + lint)
- Tarix: 19 İyun 2026

## Scope
basabas (TASK-0411) düzəlişindən sonra digər tool-larda eyni "ayraçsız böyük rəqəm" sinifi 3 paralel review agenti ilə tarandı. Təsdiqlənmiş minlik-ayraç boşluqları düzəldildi.

## Düzəlişlər (yalnız təsdiqlənmiş, böyük rəqəm)
- `app/toolkit/delivery-calc/page.tsx` — `fmt0` helper + aylıq net (`row.monthlyNet`) minlik ayraçla
- `app/toolkit/menu-matrix/page.tsx` — `fmt0` helper + orta satış (`avgSales`) ayraçla
- `components/marketinq-ocagi/yemek-xerci/YemekXerciPage.tsx` — mərkəzi `money()` helperi `Intl.NumberFormat('az-AZ')` + finite guard (bütün tool-u əhatə edir)
- `components/marketinq-ocagi/menyu-analitigi/MenyuAnalitiyiForm.tsx` — "Aylıq mənfəət" `Intl.NumberFormat`

Locale olan tool-larda `fmt0` istifadəçi dilinə uyğun ayraç verir; hardcoded AZ tool-larda `az-AZ`.

## Bu task-da DEYİL (ayrıca qeyd olundu)
- **Kontrast (CLAUDE.md):** review-də ~16 interaktiv element / muted label tapıldı (LikertScale `text-slate-300`, bəzi stat label `text-slate-400`). Ayrıca fokuslu sweep tələb edir — bu PR-a daxil deyil.
- **menyu-analitigi hardcoded AZ mətn** (Marja/Aylıq mənfəət) — next-intl-ə keçid ayrı i18n task-ıdır.
- div-by-zero iddiası (menyu-analitigi:234): `item.price &&` render guard mövcud — false positive.

## Verification
- Dəyişən 4 fayl: **0 lint error** (1 pre-existing `Trash2` unused warning, bu task-a aid deyil)
- Böyük-rəqəm ayraç sinifi bu fayllarda təmizləndi
