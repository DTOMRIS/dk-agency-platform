# TASK-0410: F2.8 — Sektor dynamic [slug] route + generateStaticParams

## Summary
Add `generateStaticParams()` to the dynamic sektor `[slug]` page so Next.js
pre-renders all valid sektor slugs at build time.

## Scope
- `app/[locale]/sektor/[slug]/page.tsx` — import `VALID_SEKTOR_SLUGS`, export `generateStaticParams()`

## Verification
- `npm run build` → 0 TS errors
- `npm run lint` → 0 new errors
- `/sektor/qonaq-evi` → 200
- `/sektor/otel` → 200
- `/sektor/restoran` → 200
- `/sektor/kafe` → 200
- `/sektor` → 200
- `/sektor/yoxdur` → 404

## Status
Complete
