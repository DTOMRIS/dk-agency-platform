# TASK-0042: feat(i18n): translate auth login page (4 languages)

## Status: completed
## Branch: feat/i18n-phase-1-core
## Date: 2026-05-04

## Description
Localize `app/auth/login/page.tsx` using Pattern A (inline `Record<Locale, {...}>` copy object).
The page lives outside `app/[locale]/` so `useTranslations()` is not available; locale is detected
from `window.location.pathname` on mount via `normalizeLocale()` from `@/i18n/config`.

## Strings translated (per locale: az / en / tr / ru)
- Page title ("Daxil ol")
- Subtitle / description
- Email label and placeholder
- Password label
- "Forgot password?" link
- Submit button ("Daxil ol və davam et")
- Loading state ("Yoxlanır...")
- "No account?" / "Sign up" footer
- "Membership info" link text
- "Go back" navigation
- "Demo logins" panel label
- Fallback error message

## Files changed
- `app/auth/login/page.tsx`
