# TASK-0034

- Scope: Pin the Node.js version to >=22 in package.json `engines` field and create `.nvmrc` with value `22`. This ensures Hostinger and local dev use the same Node version that the project was developed and tested on.
- Affected files:
  - package.json
  - .nvmrc (new)
- Do-not-touch list:
  - All other package.json fields
- Done criteria:
  - package.json contains `"engines": { "node": ">=22" }`
  - .nvmrc exists with content `22`
- Rollback steps:
  1. Remove the `engines` field from package.json
  2. Delete .nvmrc
- Owner: Dogan
- Date: 2026-05-04
