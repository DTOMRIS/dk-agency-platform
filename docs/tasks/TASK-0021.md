# TASK-0021

- Scope: Auth system — mock-dan production-a keçid
- Affected files:
  - lib/auth/jwt.ts (NEW — JWT sign/verify/cookie helpers)
  - app/api/auth/login/route.ts (NEW — real bcrypt+DB login, JWT cookie)
  - app/api/auth/register/route.ts (NEW — real bcrypt+DB register, SMTP email confirm)
  - app/api/auth/me/route.ts (NEW — JWT validate, return user data)
  - app/api/auth/logout/route.ts (NEW — clear JWT cookie)
  - app/api/auth/confirm/route.ts (NEW — email verification token check)
  - app/api/auth/route.ts (REWRITTEN — all 11 TODOs resolved, real DB ops)
  - app/dashboard/layout.tsx (MODIFIED — JWT auth guard, redirect if not auth'd)
  - package.json (MODIFIED — added jsonwebtoken)
- Do-not-touch list:
  - middleware.ts
  - lib/supabase/*
  - components/auth/*
- Smoke tests:
  - Register → DB insert ✅
  - Email confirm → emailVerified=true ✅
  - Login verified → JWT cookie ✅
  - /api/auth/me → user data ✅
  - Wrong password → 401 ✅
  - Unverified email → 403 ✅
  - Dashboard no token → redirect ✅
  - Dashboard with token → 200 ✅
  - Logout → cookie cleared ✅
