# Task Card

- ID: `TASK-0447`
- Scope: fix(dashboard): mobil daşma `blog` (başlıq sətri) + `food-cost` (tab zolağı); `hero` saxta «Saxla» redaktoru silindi (TD-005); smoke spec 390px yoxlaması 5 → bütün səhifələr
- Affected files: app/dashboard/blog/page.tsx, app/dashboard/food-cost/page.tsx, app/dashboard/hero/page.tsx (silindi) + [locale] mirror, components/dashboard/DashboardSidebar.tsx, messages/{az,ru,en,tr}.json (`dashboardSidebar.nav.hero`), lib/data/adminContent.ts (`defaultHeroContent`), e2e/dashboard-smoke.spec.ts, docs
- Do-not-touch list: PROTECTED.md — toxunulmadı; `.env*`-ə yazılmadı; `--no-verify` işlədilmədi; `hero_content` DB cədvəli qalır (schema dəyişmədi)
- Done criteria: 19 səhifə × 390px `scrollWidth <= clientWidth`; `/dashboard/hero` → 404 (kök + /tr); sidebar-da hero linki yox; i18n paritet; spec 5/5; tsc baza; eslint 0
- Rollback steps: `git revert` (PR merge commit-i); DB dəyişikliyi yoxdur
- Owner: Doğan («hallet») / Claude (qərar + icra)
- Date: 2026-09-13

## Notes
- Hero qərarı: landing `Hero.tsx` 4 dildə kodda; `hero_content`-i tətbiq oxumur; `defaultHeroContent` saxta rəqəmlər («150+ aktiv restoran», «32%») — 0444 presedenti («C sil»). Real redaktor = ayrıca feature (HANDOFF).
- Prettier PostToolUse hook 3 faylda 27/160/314 sətir kollateral verdi → geri alınıb `perl` ilə minimal tətbiq (L-051).
- Tam detal: `docs/CHANGELOG.md` və `docs/DEVLOG.md` (TASK-0447 girişi)
