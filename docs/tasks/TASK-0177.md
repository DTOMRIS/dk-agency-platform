# TASK-0177 — Production Smoke Test Bug Fixes

- Status: ✅ Tamamlandı
- Tarix: 27 May 2026
- PR: pending
- Scope: 2 critical bug fix from production smoke test

## Bug #1 — AI Readiness i18n MISSING_MESSAGE
- **Symptom:** Option labels show raw key names instead of translations
- **Root cause:** Component appended `.label` suffix to translation key, but messages don't have that suffix
- **Fix:** `AiReadinessScore.tsx` line 228: `t(\`${optKey}.label\`)` → `t(optKey)`
- **Lesson:** L-028

## Bug #2 — Severity upper-bound returns OK for extreme values
- **Symptom:** labor_cost 300% → ✅ OK, net_profit -1760% → ✅ OK
- **Root cause:** Threshold ranges capped at `max: 100`, values beyond fall through to default `'OK'`
- **Fix:** Replace `max: 100` with `max: Infinity`, `min: -100` with `min: -Infinity`, default return `'CRITICAL'`
- **Lesson:** L-027

## Bug #3 — Cookie banner not visible
- **Status:** False alarm — banner renders via PublicChrome in root layout, localStorage consent from prior test hides it
- **No code change needed**
