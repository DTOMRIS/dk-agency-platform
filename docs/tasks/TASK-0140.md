# TASK-0140 — Admin: İstifadəçi Sil + Bulk Action

**Status:** done
**Priority:** high
**Sprint:** 2026-05-17

## Scope
Soft-delete members (single + bulk) with login block, audit, and double confirm UI.

## Why Soft Delete
Hard delete breaks audit log referential integrity (targetUserId → orphan).
deletedAt timestamp preserves history while hiding from UI and blocking login.

## Schema Change
- `users.deleted_at` timestamptz (nullable)
- `member_profiles.deleted_at` timestamptz (nullable)
- Migration: drizzle/0004_aspiring_rockslide.sql

## Files Created
- `app/api/admin/members/bulk/route.ts` — DELETE batch (max 50)

## Files Modified
- `lib/db/schema.ts` — deletedAt on users + memberProfiles
- `app/api/admin/members/[id]/route.ts` — DELETE method + GET deletedAt filter
- `app/api/admin/members/route.ts` — GET deletedAt IS NULL filter
- `app/api/auth/login/route.ts` — deletedAt → 403 block
- `app/dashboard/users/[id]/page.tsx` — delete button + double confirm
- `app/dashboard/users/page.tsx` — onDelete + onBulkDelete callbacks
- `components/dashboard/MembersTable.tsx` — checkboxes + bulk action bar + delete buttons
- `e2e/admin-members.spec.ts` — DELETE 401 tests
- `messages/*.json` — 4 languages (delete/bulk keys)

## Security
- Self-delete blocked (API 403 + UI disabled)
- Bulk max 50 (DoS protection)
- Login blocked before emailVerified check
- Audit: member.deleted with name/email/role (no sensitive data)
