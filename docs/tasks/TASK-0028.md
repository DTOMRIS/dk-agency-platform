# TASK-0028

- Scope: Add `npm install --include=dev` before build command to ensure devDependencies are available on Hostinger.
- Affected files:
  - package.json
- Do-not-touch list:
  - All other config files
  - App business logic
- Done criteria:
  - package.json build script includes `npm install --include=dev &&` prefix
  - Production build installs devDependencies before compiling
- Rollback steps:
  1. Revert build script to: `node --max-old-space-size=4096 node_modules/next/dist/bin/next build`
- Owner: Dogan
- Date: 2026-05-04
