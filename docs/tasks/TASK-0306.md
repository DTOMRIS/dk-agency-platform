# TASK-0306 - Remove mock fallback from public ilanlar pages

- ID: `TASK-0306`
- Scope: Public `/ilanlar` showcase and detail pages must not show fake mock listings when the database is unavailable or empty.
- Root cause: `listings-repository.ts` returned `MOCK_LISTINGS` array when `db` was null, and `ListingsShowcasePage.tsx` initialized state with mock data and fell back to it on fetch errors.
- Affected files:
  - `lib/db/listings-repository.ts`
  - `components/listings/ListingsShowcasePage.tsx`
- Done criteria:
  - Public listing pages show clean empty state when DB has no listings.
  - No mock data appears on the live site during DB downtime.
  - Dashboard admin mock fallback remains intact for offline dev.
  - Build passes with 0 TS errors.
