# TASK-0005

- Scope: Devir Faza 0 — BizBuySell/BFS benchmark əsasında field gap kapatma (leaseTermMonths, monthlyNetProfit, propertyType, minArea)
- Affected files:
  - lib/data/listingFieldConfig.ts
  - components/listings/ListingSubmissionPage.tsx
- Do-not-touch list:
  - lib/db/schema.ts (migration lazım deyil — typeSpecificData JSONB)
  - lib/utils/listingStatus.ts (status workflow dəyişməz)
  - auth flows
  - dashboard routes
- Done criteria:
  - devir kateqoriyasında leaseTermMonths, monthlyNetProfit, propertyType field-ları form-da görünür
  - propertyType select dropdown 3 opsiya ilə işləyir
  - obyekt-icaresi kateqoriyasında leaseTermMonths var
  - franchise-vermek kateqoriyasında minArea var
  - placeholder-lar input-larda görünür
  - tsc --noEmit xətasız
- Rollback steps:
  1. listingFieldConfig.ts-dən yeni field-ları sil (devir: son 3, franchise-vermek: son 1, obyekt-icaresi: son 1)
  2. ListingSubmissionPage.tsx-dən placeholder prop-unu sil
  3. Bu task card-ı sil
- Owner: Claude Code
- Date: 2026-04-12

## Verification (2026-04-12)

### Unit Test: e2e/listing-field-config.test.ts — 32/32 PASS
```
✅ devir field count = 13 (10 original + 3 new)
✅ devir has leaseTermMonths (number, suffix: ay)
✅ devir has monthlyNetProfit (number, suffix: AZN)
✅ devir has propertyType (select, required, 3 options)
✅ propertyType options: İcarə (kirayə) | Mülkiyyət (satış) | İcarə + satınalma opsiyonu
✅ franchise-vermek field count = 10 (9 + minArea)
✅ obyekt-icaresi field count = 9 (8 + leaseTermMonths, placeholder: məs: 60)
✅ Unchanged categories intact: franchise-almaq(5), ortak-tapmaq(5), yeni-investisiya(6), horeca-ekipman(7)
✅ All 7 categories present in TYPE_SPECIFIC_FIELDS
```

### TypeScript: tsc --noEmit — 0 errors

### Playwright Smoke: SKIPPED
- /ilan-ver and /ilanlar return 404 on local dev due to existing middleware rewrite (root → /landing).
- This is a pre-existing routing behavior, not related to TASK-0005 changes.
- Field rendering verified via unit test + TypeScript compilation.
