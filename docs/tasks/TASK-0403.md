# TASK-0403 - GitHub Actions cron for news pipeline + failure alert

- ID: `TASK-0403`
- Scope: Automate news:fetch + news:synthesize via GitHub Actions cron (2x daily). Failure sends SMTP alert to CTO. Hostinger untouched — no build/deploy triggered.
- Affected files:
  - `.github/workflows/news-ingest.yml` (new — cron + workflow_dispatch + failure mail)
- NOT touched: Hostinger, app code, schema, existing RSS workflow
- Prerequisites: GitHub Secrets — `DATABASE_URL`, `NEWSDATA_API_KEY`, `DEEPSEEK_API_KEY`, `SMTP_USER`, `SMTP_PASS`
- Done criteria:
  - workflow_dispatch manual trigger → Actions green → new drafts in Neon
  - Intentional failure → CTO email arrives
  - Hostinger not triggered (no deploy, no 503)
  - Schedule: 06:00 + 14:00 UTC (10:00 + 18:00 Baku)
