# TASK-0002

- Scope:
  - Restore locale admin leads access by wiring `/[locale]/admin/leads` to the existing KAZAN leads admin screen.
  - Fix article/blog readability regressions caused by light-surface contrast issues and add a hard guardrail for future agents.
- Affected files:
  - app/[locale]/admin/leads/page.tsx
  - CHANGELOG.md
  - CLAUDE.md
  - app/[locale]/blog/page.tsx
  - app/[locale]/blog/[slug]/page.tsx
  - app/haberler/[slug]/page.tsx
  - components/blog/BlogSidebars.tsx
  - docs/tasks/TASK-0002.md
- Do-not-touch list:
  - KAZAN lead DB schema
  - existing dashboard leads UI implementation
  - auth adapter and member session flow
  - database schema and migrations
  - unrelated dashboard routes
- Done criteria:
  - `/az/admin/leads` no longer returns 404
  - `/en/admin/leads` no longer returns 404
  - route reuses existing real DB-backed KAZAN leads screen
  - changelog entry added
  - blog listing route uses explicit dark text on light surfaces
  - blog detail route uses explicit dark text on article and sidebar light cards
  - news detail route uses explicit dark text on article and related-news light cards
  - blog sidebar widgets used on light pages no longer rely on dark-surface tokens
  - `CLAUDE.md` documents a release-blocking contrast rule for blog/news/article surfaces
- Rollback steps:
  1. Remove `app/[locale]/admin/leads/page.tsx`
  2. Remove TASK-0002 changelog entry
  3. Revert the article/blog readability files listed above
  4. Delete this task card
- Owner: Codex
- Date: 2026-04-12
