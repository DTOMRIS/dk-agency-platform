# TASK-0102
- Scope: Transform contact page from visible phone card into lead funnel.
- Affected files: `app/[locale]/elaqe/page.tsx`, `components/contact/ContactFunnel.tsx`, `app/api/leads/track/route.ts`, `app/api/leads/whatsapp/route.ts`, `components/kazan-ai/FloatingKazanWidget.tsx`, `lib/db/schema.ts`, `messages/*.json`, `drizzle/0001_foamy_gideon.sql`, `e2e/contact-funnel.spec.ts`.
- Protected files untouched: `lib/member-access.ts`, `listingFieldConfig.ts`, `middleware.ts`.
- Lead store: generic `leads` table with `source`, `channel`, `locale`, `user_agent`, `ip_hash`, and `created_at`.
- Channels: `kazan`, `whatsapp`, `telegram`; all write `source='contact_page'`.
- Telegram handle: `https://t.me/dkagency`.
- WhatsApp opens through a same-origin redirect so the phone number is not rendered by the contact page.
- Done criteria: phone card removed; KAZAN AI / WhatsApp / Telegram cards render in 4 languages; CTA clicks POST to `/api/leads/track`; WhatsApp has localized prefill; KAZAN AI opens with contact context.
- Env note: `IP_HASH_SALT` must be added in Hostinger before deploy.
- Owner: Dogan
- Date: 2026-05-09
