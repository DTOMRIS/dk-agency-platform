# TASK-0304 - News editor spine

- ID: `TASK-0304`
- Scope: Replace the incomplete news list modal with a dedicated full-page editor that supports complete article updates, cover image add/remove, delete, translation, preview, and publishing.
- Affected files:
  - `app/dashboard/xeberler/page.tsx`
  - `app/dashboard/xeberler/[id]/page.tsx`
  - `app/[locale]/dashboard/xeberler/[id]/page.tsx`
  - `components/dashboard/NewsEditorForm.tsx`
  - `app/api/news/admin/[id]/route.ts`
  - `lib/repositories/newsRepository.ts`
- Do-not-touch list: Existing user changes in `.claude/agent-memory/scout/*` and `lib/news/rss-pipeline.ts`; protected files; ingestion pipeline.
- Done criteria:
  - News list edit action opens a dedicated page, not a modal.
  - Existing article data loads into the shared editor without field loss.
  - Saving uses PATCH and preserves slug, source, publication date, type, flags, SEO, four locales, and cover image.
  - Cover image can be added, replaced, or removed.
- Existing article can be previewed, translated, published, or deleted from the edit page.
- Server-rendered article loading requires the same authenticated admin plan as the API.
- Auth gate, lint, build, and focused route smoke checks pass.
- Rollback steps: Revert the TASK-0304 commit and remove the two edit route files.
- Owner: Codex
- Date: 2026-06-12

## Verification

- `npm run lint`: PASS, 0 errors.
- `npm run build`: PASS; root and locale edit routes registered.
- Local production smoke: expected `307` route redirects and `403` unauthenticated API gates.
- `npm run verify:staged`: PASS.
- DK validator: PASS 8/8 via Git Bash.
