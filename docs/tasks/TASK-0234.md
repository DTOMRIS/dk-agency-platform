# Task Card

- ID: `TASK-0234`
- Scope: Launch sprint block 1/9 — add the DK mobile logo and localized tagline to the dark navigation zone; convert the home hero to the dark Zona 1 treatment and replace the right illustration with a responsive photo placeholder plus localized floating stat card.
- Affected files:
  - `components/layout/Header.tsx` (PROTECTED; explicitly approved for logo/nav presentation only)
  - `components/Hero.tsx`
  - `messages/{az,en,ru,tr}.json`
  - `docs/CHANGELOG.md`
  - `docs/DEVLOG.md`
- Do-not-touch list: auth/member session logic, `lib/member-access.ts`, `middleware.ts`, Footer, layout, package/config files.
- Done criteria: logo renders through `next/image`; nav and hero use the dark brand zone; hero right panel is desktop-only; stat and placeholder copy resolve in all four locales; build and locale smoke checks pass.
- Rollback steps: `git revert` the TASK-0234 commit.
- Owner: Doğan / Codex
- Date: 2026-06-07

## Notes

- The live home route uses `components/Hero.tsx`; `components/home/HeroSection.tsx` is not used by `app/[locale]/page.tsx`.
- No real `hero-placeholder.jpg` exists, so the approved CSS gradient fallback is used.
- `+34%` remains an explicitly temporary localized value until Doğan provides a verified production metric.
