# Task Card

- ID: `TASK-0234`
- Scope: Launch sprint block 1/9 — apply the AI Studio light navbar and hero fullspec, including the DK logo block, five desktop links, two CTAs, localized hero copy, responsive photo fallback, and floating stat card.
- Affected files:
  - `components/layout/Header.tsx` (PROTECTED; explicitly approved for logo/nav presentation only)
  - `components/Hero.tsx`
  - `messages/{az,en,ru,tr}.json`
  - `docs/CHANGELOG.md`
  - `docs/DEVLOG.md`
- Do-not-touch list: auth/member session logic, `lib/member-access.ts`, `middleware.ts`, Footer, layout, package/config files.
- Done criteria: logo renders through `next/image`; navbar and hero use the exact fullspec sizes/colors; hero image panel stacks below copy on mobile; stat and placeholder copy resolve in all four locales; build and locale smoke checks pass.
- Rollback steps: `git revert` the TASK-0234 commit.
- Owner: Doğan / Codex
- Date: 2026-06-07

## Notes

- The live home route uses `components/Hero.tsx`; `components/home/HeroSection.tsx` is not used by `app/[locale]/page.tsx`.
- No real `hero-placeholder.jpg` exists, so the approved CSS gradient fallback is used.
- Header auth/member session, logout, profile dropdown, and existing mobile-menu behavior remain unchanged.
