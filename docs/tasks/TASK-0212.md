# Task Card

- ID: `TASK-0212`
- Scope: Resolve TD-003 — convert `app/b2b-panel/page.tsx` from Pattern C (inline `Record<Locale>` pageCopy) to Pattern A (`useTranslations`), per L-004/L-009.
- Affected files:
  - `app/b2b-panel/page.tsx` (removed ~180-line pageCopy + normalizeLocale/usePathname logic; now `useTranslations('b2bPanel')`)
  - `messages/{az,ru,en,tr}.json` (+ `b2bPanel` namespace — strings moved verbatim, no new translation)
  - `docs/TECH_DEBT.md` (TD-003 → resolved), `docs/CHANGELOG.md`
- Do-not-touch list: all PROTECTED files. (Header/Footer not touched.)
- Done criteria: no `pageCopy`/`normalizeLocale` left in the page; build 0 error; all b2bPanel keys present in 4 langs (no MISSING_MESSAGE); `/b2b-panel` + `/en/b2b-panel` 307→200 (guest guard intact), no 500.
- Rollback steps: `git revert` the commit.
- Owner: Doğan / Claude
- Date: 2026-06-07

## Notes
- Low-risk approach: a local `copy` object built from `t()`/`t.raw()` mirrors the old shape, so all 23 downstream `copy.X` usages stay unchanged.
- Verified: build PASS, key-completeness check across 4 langs PASS, route smoke 307→200.
- Authenticated render not smoked (sandbox has no DATABASE_URL/session); key-completeness is the MISSING_MESSAGE guard.
