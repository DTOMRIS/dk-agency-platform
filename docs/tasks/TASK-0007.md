# TASK-0007 — Devir M5: Mock → Real DB CRUD Migration

**Status:** Done
**Date:** 2026-04-12
**Branch:** feat/devir-m5-real-crud

## Scope

Migration of all listing-related UI components from mock data fallbacks to real Neon PostgreSQL DB reads/writes.

## Affected Files

### New
- `lib/types/listing.ts` — Unified `Listing` type (replaces `MockListing`)

### Modified
- `components/listings/ListingSubmissionPage.tsx` — Real POST /api/listings, trackingCode from server, error state
- `components/listings/ListingsShowcasePage.tsx` — Mock fallback removed, empty state on API error
- `components/listings/ListingModal.tsx` — Uses `Listing` type from lib/types/listing
- `components/listings/ListingCard.tsx` — Uses `Listing` type
- `components/listings/LeadForm.tsx` — Real POST /api/listings/[id]/leads, error handling, removed client-side sendEmail
- `components/dashboard/DashboardSidebar.tsx` — Pending listing count from real DB API
- `app/dashboard/ilanlar/page.tsx` — Mock catch fallback removed, uses `Listing` type
- `app/dashboard/ilanlar/[id]/page.tsx` — Mock catch fallback removed, uses `Listing`/`ListingReviewNote`/`ListingLead` types
- `lib/repositories/listingRepository.ts` — Uses `Listing` type, mock fallbacks removed from all functions
- `lib/db/listings-repository.ts` — Uses `Listing` type, mock fallback removed

### Deprecated (not deleted — backward compat with seed.ts)
- `lib/data/mockListings.ts` — MOCK_LISTINGS array kept for seed.ts only, @deprecated added

## Done Criteria

- [x] ListingSubmissionPage.tsx sends real POST /api/listings
- [x] ListingsShowcasePage.tsx reads real DB, no mock fallback
- [x] ListingModal.tsx uses real Listing type
- [x] LeadForm.tsx sends real POST /api/listings/[id]/leads
- [x] Admin dashboard reads real DB, no mock catch fallback
- [x] `MockListing` type aliased to `Listing` in lib/types/listing.ts
- [x] No production file imports from @/lib/data/mockListings
- [x] TypeScript: 0 errors (npx tsc --noEmit passes)

## Rollback

Revert branch `feat/devir-m5-real-crud`. The `mockListings.ts` data is preserved in the file, so seed.ts still works.

## TODOs (future tasks)

- File upload: real Cloudinary/R2 storage (images currently stored as local object URLs which are lost on reload)
- Email notification on listing submit: server-side Resend integration (console.log placeholder in /api/listings/[id]/leads)
- Lead status PATCH: `/api/listings/[id]/leads/[leadId]` route for marking leads as contacted
- `mockListings.ts` full cleanup after seed.ts is updated to use its own seed data
