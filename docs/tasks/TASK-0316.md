# TASK-0316

- ID: `TASK-0316`
- Scope: Banner ad system PR-2 — public `<AdSlot>` render + impression/click tracking + first slot placement.
- Reason: PR-1 (TASK-0315) shipped schema + admin CRUD; ads were created but never rendered or tracked. This makes them live + measurable.
- Affected files:
  - `components/ads/AdSlot.tsx`: NEW server component — fetches active ads for a placement, renders one. Returns null when none (pages tolerate absence). Rotation is server-side (`ORDER BY random()`), no impure call in render.
  - `components/ads/AdView.tsx`: NEW client component — renders image/gif (`<img>`) or video (`<video autoplay muted loop playsinline>`), fires impression beacon once on mount, wraps creative in click-tracking link, shows a "Reklam" disclosure badge.
  - `app/api/ads/[id]/impression/route.ts`: NEW POST — best-effort impression counter, always 204.
  - `app/api/ads/[id]/click/route.ts`: NEW GET — increments click + 302 to target; open-redirect guard (http/https only, else homepage).
  - `lib/repositories/adsRepository.ts`: reads + increments wrapped in try/catch (never break a public page if table missing); public query now `ORDER BY random()`.
  - `app/haberler/[slug]/page.tsx`: `<AdSlot placement="news-sidebar" />` added to the detail sidebar.
- Do-not-touch list: next.config.ts (CSP) — html5/lottie deferred to PR-3.
- Done criteria: with an active `news-sidebar` ad, the news detail page shows it with a "Reklam" badge; clicking redirects via /api/ads/[id]/click and increments clicks; impression increments on view. No ads / no table → news page renders normally. tsc + lint clean.
- Rollback steps: remove the AdSlot import+usage from the news page; delete the new components/routes.
- Follow-up: more placements (home-mid, blog sidebars); PR-3 = CSP + html5/lottie.
