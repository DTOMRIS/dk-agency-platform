# TASK-0154 - Pulsuz Qeydiyyat-Gate (Blog + Xəbərlər)

Status: DONE

## Scope
- Extended existing 40% scroll registration gate from blog to news articles (haberler/xeberler).
- Refactored BlogContentWrapper: hardcoded AZ strings → `useTranslations('registrationGate')` (4 locales).
- Gate messaging changed from "paywall" language to "pulsuz qeydiyyat" (free registration wall).
- Modal color changed from red (payment) to emerald (free/positive).
- Removed "Member Flow MVP" developer note from production UI.
- `lib/member-access.ts` NOT modified (PROTECTED).

## Files Changed
- `components/news/BlogContentWrapper.tsx` — i18n refactor + emerald UI
- `app/haberler/[slug]/page.tsx` — wrapped with BlogContentWrapper
- `messages/{az,en,tr,ru}.json` — `registrationGate` namespace (13 keys each)
- `CHANGELOG.md`, `DEVLOG.md`

## Route Coverage
- `/haberler/[slug]` — directly modified
- `/xeberler/[slug]` — re-exports from haberler (covered)
- `/[locale]/haberler/[slug]` — re-exports from haberler (covered)
- `/[locale]/blog/[slug]` — already used BlogContentWrapper (updated messaging)

## Follow-up
- TASK-0155: Slug mismatch fix
- TASK-0156: Config file physical reorg

## Verification
- Build: 0 errors
- member-access.ts: untouched (git diff verified)
- Hardcoded AZ in BlogContentWrapper: 0 hits
