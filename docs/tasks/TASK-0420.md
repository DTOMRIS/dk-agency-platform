# TASK-0420: Onboarding modal mobile polish (P1)

## Description
Follow-up to TASK-0419. P0 already delivered the critical mobile fixes (sticky CTA
footer, scrollable body, single-column grid). This pass adds the remaining small
mobile/accessibility polish to `OnboardingModal.tsx` only — no structural or design
change, and deliberately NOT the "selected-first reorder" (rejected: reordering cards
under the user's thumb is a known UX anti-pattern).

## Changes
- `components/onboarding/OnboardingModal.tsx`
  - Responsive padding: scroll body `p-5 sm:p-8`, footers `px-5 py-4 sm:px-8 sm:py-5`
    (more usable width on small phones).
  - Tap targets ≥44px: `min-h-11` on Skip, Save, notify, and go-to-dashboard buttons.
  - Contrast: Skip button `text-slate-500` → `text-slate-600`; helper hints
    (pickCount, whyWeAsk) `text-slate-400` → `text-slate-500` (WCAG, matches TASK-0413).

## Not touched
Logic, state, i18n keys, imports — unchanged (className-only edits). No PROTECTED files,
no routes, no other component.

## Verification
- `verify-encoding` → PASS
- `tsc --noEmit` → 0 errors in OnboardingModal.tsx
- No `any` / `console.log`
- Build (full) deferred to CI — sandbox `npm install` blocked by xlsx CDN egress (infra),
  same as TASK-0419; CI quality-gates builds it.

## Status
Completed (code); CI builds on PR.
