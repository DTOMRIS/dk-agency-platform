# Task Card

- ID: `TASK-0224` (LAUNCH-P0)
- Scope: Hide mock/half-finished admin surfaces and remove unverified/fake stats before launch, so nothing reads as broken or fabricated.
- Affected files:
  - `components/home/StatsBarSection.tsx` — returns null (was unverified vanity numbers: "6.7M impressions", "99.9% availability")
  - `components/dashboard/DashboardSidebar.tsx` — removed `auditor`, `invoices`(faturalar), `site` nav items (mock data / no-op save) + dropped now-unused icon imports
  - `app/dashboard/site/page.tsx` — both "save" buttons disabled (previously showed a fake "saved" toast with no persistence)
  - `app/b2b-panel/page.tsx` — STATS set to 0 / "—" (honest empty state instead of fabricated 4/2847/8/15)
  - `docs/CHANGELOG.md`
- Do-not-touch list: PROTECTED files untouched. (`/dashboard/ayarlar` + `/dashboard/settings` remain as the real settings pages.)
- Done criteria: build 0 error; no fake stats on homepage or b2b panel; auditor/faturalar/site hidden from admin nav; site save no longer claims success.
- Rollback steps: `git revert`.
- Owner: Doğan / Claude
- Date: 2026-06-07

## Notes
- These surfaces are not deleted — only hidden. Re-enable each once its backend is real (auditor persistence, invoice API/UI, site-settings save). PR-1 (#318) already secured the invoice API.
- No new hardcoded strings (L-004): buttons disabled via style, labels unchanged.
