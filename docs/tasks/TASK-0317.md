# TASK-0317

- ID: `TASK-0317`
- Scope: Place `<AdSlot placement="blog-sidebar" />` on the blog detail page (completes ad slot coverage: news + blog).
- Reason: PR-2 (TASK-0316) shipped AdSlot + tracking with one slot (news-sidebar). Blog is the highest-traffic content; it needs a slot too.
- Affected files:
  - `app/[locale]/blog/[slug]/page.tsx`: import AdSlot; render `<AdSlot placement="blog-sidebar" />` at the top of the sticky sidebar. Root-mirror `app/blog/[slug]/page.tsx` re-exports → inherits automatically.
- Do-not-touch list: next.config.ts (CSP) — html5/lottie still PR-3.
- Done criteria: with an active blog-sidebar ad, it renders atop the blog sidebar; no ad → sidebar unchanged. tsc + lint clean.
- Rollback steps: remove the import + AdSlot line.
