# TASK-0424: Blog/news UX round 2 — mobile menu, paywall off, carousel counter

## Changes (per owner decisions on live-site review)
- **A — Blog in mobile menu:** added a top-level `Blog` link to the mobile hamburger
  menu (`components/layout/Header.tsx`) so it's discoverable without expanding the
  "Resurslar" dropdown. Desktop nav unchanged.
- **C — 40% paywall disabled:** everyone reads full blog + news articles now.
  `components/news/BlogContentWrapper.tsx` gained a `PAYWALL_ENABLED = false` flag
  (single switch to re-enable) so `shouldGate` is always false — no scroll-lock modal.
  Blog detail page now serves full content (`getProtectedArticleContent(..., false)`).
  Note: the news article page passed `isPremium` (always true), so ALL news was gated
  at 40%; the wrapper flag disables it globally. Registration stays encouraged via
  existing on-page CTAs (toolkit, newsletter).
- **Extra — MansetVitrin `4 / 8` counter:** looked cramped/wrapped on mobile. Counter
  is now `whitespace-nowrap shrink-0`, the dots row is hidden on mobile (`hidden sm:flex`)
  to make room, and the nav gap tightens on small screens (`gap-2 sm:gap-4`).

## Still open (needs owner confirm — separate)
- **B — homepage "Sektordan ən son yeniliklər" section** (`components/NewsPreview.tsx`)
  uses hardcoded placeholder `NEWS_ITEMS`, which is why the news-labelled block shows
  blog-like/stale content. Fixing it means wiring it to real news (newsRepository) and
  linking to individual `/haberler/<slug>` articles — a data change to confirm first.

## Verification
- `tsc --noEmit` → 0 errors in changed files
- Minimal diff (13 lines); formatter reflow deliberately avoided
- Build deferred to CI (sandbox xlsx egress block)

## Status
A / C / Extra completed (code); B pending owner confirm.
