# TASK-0152 - Pricing Page

Status: DONE

## Scope
- Added `/[locale]/pricing` as a static public pricing page.
- Added `components/pricing/PricingPage.tsx` as the single pricing component.
- Used `lib/marketing-tools-config.ts` as the source of truth for tier tool lists and counts.
- Added Pattern A i18n namespace `pricing` in AZ, EN, RU, and TR.

## Notes
- No DB, payment integration, or AI layer was added.
- Current config contains 3 SAGIRD tools, 12 KALFA tools, and 6 USTA tools. The page renders counts dynamically from config instead of copying stale prompt counts.
- SAGIRD CTA points to the existing localized auth register route.
- KALFA and USTA CTA links use `NEXT_PUBLIC_WHATSAPP_NUMBER` with a safe fallback.

## Verification
- Build, typecheck, Playwright smoke, and validator results are recorded in the PR summary.

