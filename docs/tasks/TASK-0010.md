# TASK-0010

- Scope: Domain dkagency.az — Vercel domain qoşulması üçün baseUrl hazırlığı
- Branch: chore/domain-dkagency
- Status: done

## Dəyişən fayllar
- `app/sitemap.ts` — BASE_URL hardcode → `process.env.NEXT_PUBLIC_SITE_URL || 'https://dkagency.az'`
- `app/robots.ts` — SITE_URL env var-dan, sitemap + host dinamik
- `app/api/rss/haberler/route.ts` — baseUrl env var-dan
- `e2e/prod-verify.spec.ts` — PROD → `process.env.PLAYWRIGHT_BASE_URL || 'https://dkagency.az'`
- `.env.example` — NEXT_PUBLIC_SITE_URL default dkagency.az oldu
- `docs/ENV-SETUP.md` — Domain Setup bölməsi əlavə edildi (DNS, Vercel, SSL addımları)

## Dəyişməyən fayllar
- `next.config.ts` — hardcode URL yoxdur, dəyişiklik lazım deyil
- `app/api/listings/[id]/status/route.ts` — artıq env var istifadə edir
- `app/api/listings/[id]/leads/route.ts` — artıq env var istifadə edir
- Auth flows, DB schema, middleware

## Doğan üçün əl ilə addımlar
1. Vercel Dashboard → Project → Settings → Domains → `dkagency.az` əlavə et
2. DNS provayderindən:
   - A record: `@` → `76.76.21.21`
   - CNAME: `www` → `cname.vercel-dns.com`
3. Vercel → Environment Variables → `NEXT_PUBLIC_SITE_URL=https://dkagency.az`
4. Redeploy (yeni env var aktiv olsun)

## Test
```bash
# Sitemap yoxla
curl https://dkagency.az/sitemap.xml | head -20

# Robots yoxla
curl https://dkagency.az/robots.txt

# RSS yoxla
curl https://dkagency.az/api/rss/haberler | head -10

# E2E prod test
PLAYWRIGHT_BASE_URL=https://dkagency.az npx playwright test e2e/prod-verify.spec.ts
```
