# TASK-0017

- Scope: Commit pending restaurant auditor files
- Affected files:
  - app/api/audit/ (NEW — audit API routes)
  - app/api/audit/[id]/route.ts (single audit CRUD)
  - app/dashboard/auditor/page.tsx (NEW — auditor admin panel)
  - lib/audit/ (NEW — photo-analyzer, menu-analyzer, full-audit)
  - lib/repositories/auditRepository.ts (NEW — audit DB repository)
  - components/analytics/ (NEW — analytics components)
- Do-not-touch list:
  - auth flows
  - middleware.ts
  - DB schema (auditor tables already in schema)
