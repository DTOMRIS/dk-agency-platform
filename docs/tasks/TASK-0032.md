# TASK-0032

- Scope: Extract a `getBaseUrl()` helper into `lib/utils/get-base-url.ts` that returns `NEXT_PUBLIC_APP_URL` with a fallback to `https://dkagency.com.tr`. This is the single source of truth for the public base URL used across auth routes and email templates.
- Affected files:
  - lib/utils/get-base-url.ts (new)
- Do-not-touch list:
  - All existing utilities
  - App business logic
- Done criteria:
  - lib/utils/get-base-url.ts exists and exports `getBaseUrl()`
  - All auth routes import from this utility instead of reading env vars inline
- Rollback steps:
  1. Delete lib/utils/get-base-url.ts
  2. Restore inline env var reads in auth routes
- Owner: Dogan
- Date: 2026-05-04
