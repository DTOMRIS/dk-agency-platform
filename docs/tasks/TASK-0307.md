# TASK-0307 - Admin listing create (dashboard gate)

- ID: `TASK-0307`
- Scope: Add admin listing creation capability to `/dashboard/ilanlar`. Motor (CRUD, moderasyon, AI, schema) is fully intact — only a gate (route + button + admin extras) is added.
- Affected files:
  - `app/dashboard/ilanlar/yarat/page.tsx` (new — admin create route)
  - `app/dashboard/ilanlar/page.tsx` (add "Yeni elan yarat" button)
  - `components/listings/CreateListingForm.tsx` (add `isAdmin` prop, admin controls, bulk image delete, video embed)
  - `app/api/listings/route.ts` (accept admin fields: initialStatus, isShowcase, isFeatured)
- NOT touched: DB schema, migrations, moderasyon workflow, AI analysis, member CRUD, listingFieldConfig
- Done criteria:
  - Admin can create listing from `/dashboard/ilanlar/yarat`
  - Admin can set initial status (submitted / committee_review / showcase_ready)
  - Admin can toggle isFeatured / isShowcase at creation time
  - Admin can add internal note
  - Video embed (YouTube/Instagram link) with preview
  - Bulk image delete with checkbox selection
  - No listing limit enforced for admin (already handled by API)
  - Build + lint pass with 0 errors
