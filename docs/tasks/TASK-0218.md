# Task Card

- ID: `TASK-0218`
- Scope: Add an FAQ section to blog-29 (trademark / "Starbucks şansı, Burger King dərsi" article) so it gets an FAQPage (TASK-0217 AEO) and a visible FAQ block. Content is DB-first (L-037) → a targeted, idempotent script the owner runs locally where the DB is reachable.
- Affected files:
  - `scripts/blog-29-faq.mjs` (new — appends `## Suallar və cavablar` to blog_posts.content_az for the blog-29 slug)
  - `docs/CHANGELOG.md`
- Do-not-touch list: all PROTECTED files. No schema change (content text update only).
- Done criteria: script syntax valid; idempotent (skips if FAQ already present); inserts before the "DK Agency necə kömək edir?" CTA; FAQ matches the AEO parser format (## Suallar + `### question?`).
- How to apply (owner, local — sandbox has no DATABASE_URL):
  `node --env-file=.env.local scripts/blog-29-faq.mjs`
- Rollback steps: re-edit the DB content (remove the FAQ block) or `git revert` the script.
- Owner: Doğan / Claude
- Date: 2026-06-07

## Notes
- 5 Q&A grounded in the article's own facts (Burger King → Hungry Jack's, territorial rights, brand squatting, Starbucks = exception, where to start). No new legal claims (no agency named — consistent with the article's "ümumi məlumatdır" note).
- FAQPage emission requires TASK-0217 (PR #305) deployed; this script makes the FAQ content exist in the DB.
- If the real DB slug differs from `marka-starbucks-sansi-burger-king-dersi`, the script reports "not found" — update SLUG and re-run.
