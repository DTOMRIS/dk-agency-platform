# Task Card

- ID: `TASK-0220`
- Scope: Lead notification + admin dashboards — WhatsApp/Telegram email bildirişi, franchise leads paneli, contact tracking paneli.
- Affected files:
  - `app/api/leads/track/route.ts` (add admin email for whatsapp/telegram channel)
  - `app/dashboard/franchise-leads/page.tsx` (new — franchise leads admin panel)
  - `app/[locale]/dashboard/franchise-leads/page.tsx` (new — locale re-export)
  - `app/dashboard/contact-tracking/page.tsx` (new — contact tracking admin panel)
  - `app/[locale]/dashboard/contact-tracking/page.tsx` (new — locale re-export)
- Do-not-touch list: middleware.ts, lib/member-access.ts, lib/listingFieldConfig.ts
- Done criteria: WhatsApp/Telegram clicks trigger admin email; franchise leads dashboard renders; contact tracking dashboard renders; build passes.
- Rollback steps: `git revert` the commit.
- Owner: Doğan / Claude
- Date: 2026-06-08
