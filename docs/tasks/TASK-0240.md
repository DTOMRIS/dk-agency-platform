# TASK-0240

- Scope: forbidden word 'Tezliklə' in messages/az.json → 'Yaxında'
- Reason: TASK-0239 (#328) was squash-merged before the messages/az.json i18n
  fix landed, so the forbidden word (CLAUDE.md) was still LIVE in main
  (coming_soon + plannedToolsLabel, marketinq-ocagi rendered it). Verified
  on a local dev server via minted-JWT HTML check.
- Affected: messages/az.json (2 strings)
- DoD: grep 'Tezliklə' → 0; JSON valid; build PASS.
