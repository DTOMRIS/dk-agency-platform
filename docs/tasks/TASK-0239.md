# TASK-0239

- Scope: Portal polish — çalışmayan çıxış (logout) düymələri, b2b mock data təmizliyi, admin sidebar-dan üzv aləti (Food Cost) çıxarılması
- Bugs fixed:
  1. **Çıxış işləmirdi** — həm admin (`DashboardSidebar`) həm b2b (`B2BSidebar`) çıxış düymələrinin `onClick`-i yox idi (ölü düymə). İndi `/api/auth/logout` POST → `/auth/login` redirect + `router.refresh()`. `loggingOut` state ilə ikiqat klik qorunması.
  2. **b2b mock data** — `app/b2b-panel/mesajlar/page.tsx` 5 uydurma mesaj (Fine Dining Bakü, Ön Değerleme, Potansiyel Yatırımcı...) sərt kodlanmışdı → boş array, real empty state (`noMessages`). "AI Hesabatları" statının sərt `2`-si → real sayıma bağlandı.
  3. **b2b sidebar mock badge** — `incomingOffers: 3` və `messages: 5` sərt badge-ləri silindi (uydurma saylar).
  4. **Food Cost admin-dən çıxdı** — üzv alətidir, B2B portalda olmalıdır (Doğan tələbi).
- Affected files:
  - components/dashboard/DashboardSidebar.tsx (logout + foodCost çıxar + CookingPot import)
  - components/b2b-panel/B2BSidebar.tsx (logout + mock badge çıxar)
  - app/b2b-panel/mesajlar/page.tsx (mock → boş)
- Do-not-touch: app/layout.tsx, Header/Footer, schema.ts, middleware.ts
- DoD: build PASS · lint 0 error · çıxış real işləyir · b2b-də mock qalmadı.
