# Task Card

- ID: `TASK-0217`
- Scope: AI SEO Faza 2 — brand-entity + FAQPage structured data. Add site-wide Organization + WebSite JSON-LD on the homepage (immediate GEO value, no content dependency) and auto-emit FAQPage on blog posts that contain an FAQ section (highest AEO impact). Builds on the TASK-0215 structured-data module.
- Affected files:
  - `lib/seo/structured-data.ts` (+ `websiteNode`, `extractFaqFromMarkdown`)
  - `components/seo/SiteJsonLd.tsx` (new — Organization + WebSite, static, SSR)
  - `app/[locale]/page.tsx` (render `<SiteJsonLd />`)
  - `app/[locale]/blog/[slug]/page.tsx` (add `faqNode(extractFaqFromMarkdown(content))` to the @graph)
  - `docs/CHANGELOG.md`
- Do-not-touch list: all PROTECTED files.
- Done criteria: build 0 error; homepage emits Organization + WebSite @graph; a blog post with an FAQ section emits FAQPage (no FAQ section → no FAQPage node, graceful).
- Rollback steps: `git revert`.
- Owner: Doğan (CTO) / Claude
- Date: 2026-06-07

## Notes
- No blog post currently has an FAQ section (0 found) → the FAQ parser is ready and lights up automatically as FAQ sections are added (## Suallar / Sual-Cavab / FAQ with `### question` or `**question?**`).
- SearchAction/SiteLinksSearchBox omitted (no confirmed site-search endpoint — avoid pointing to a 404).
- FAQ answer cleaning preserves in-word hyphens (AZ suffixes like "28-32%-dir").
