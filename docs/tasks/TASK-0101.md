# TASK-0101
- Scope: Fix locale switcher links so default AZ can override stale locale cookies.
- Affected files: i18n/config.ts
- Root cause: `switchLocalePath` used prefixless default-locale URLs, so `/haqqimizda` could be redirected back to `/ru/haqqimizda` when `NEXT_LOCALE=ru` was still present.
- Done criteria: Switching from `/ru/haqqimizda` to AZ generates `/az/haqqimizda`, allowing middleware to set `NEXT_LOCALE=az`.
- Owner: Dogan
- Date: 2026-05-09
