# TASK-0013

- Scope: Fix RSS pipeline — copy title/summary to title_en/summary_en on fetch + backfill 40 records
- Root cause: createFetchedNewsArticle writes title but not title_en, losing EN original
- Affected files:
  - lib/repositories/newsRepository.ts (createFetchedNewsArticle)
  - scripts/backfill-news-en-az.mjs (NEW — one-time backfill)
- Do-not-touch list:
  - news_articles schema (columns already exist)
  - translate pipeline (separate concern)
