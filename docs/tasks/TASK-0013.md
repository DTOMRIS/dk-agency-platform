# TASK-0013

- Scope: News batch approval — script to bulk approve translated articles
- Status: ✅ Complete
- Date: 2026-04-12

## Changed files:
- `scripts/approve-translated-news.ts` — NEW: batch approve script
- `package.json` — added "approve-news" script

## Implementation:
- Query: articles with status='translated' AND titleAz != null ✅
- Action: update status → 'approved' for all matching ✅
- Modes: --dry-run (default) and --execute flags ✅
- Console output: article count + sample IDs in dry-run, summary in execute ✅

## Verification:
- tsc --noEmit: 0 errors ✅
- npm run build: ✅ Script included ✅
- Graceful DB handling: logs if DATABASE_URL missing ✅

## Usage:
```bash
# Dry run (preview)
npm run approve-news

# Execute approval
npm run approve-news --execute
```

## Testing:
- Manual: npm run approve-news → shows articles to be approved
- Manual: npm run approve-news --execute → updates DB if available

## Schema notes:
- Table: news_articles
- Columns: id, title, titleAz, status (enum: fetched|translated|approved|rejected)
- Condition: status='translated' AND titleAz IS NOT NULL

## Do-not-touch:
- news_articles table structure (no schema changes)
- status enum definitions

## Owner:
- claude-code (builder)
