# Task Card

- ID: `TASK-0211`
- Scope: Optimize the oversized `logo-mobil.png` asset (1.2 MB, 1254×1254) that was added in the previous feature merge. Resize to 144×144 and compress so the Header/Footer/B2B logo stops shipping >1 MB on every page load.
- Affected files:
  - `public/images/logo-mobil.png` (asset only — resized 1254→144, 1.2 MB → ~9 KB)
  - `docs/CHANGELOG.md`
- Do-not-touch list: all PROTECTED files. NOTE: the logo is referenced by `<img>` in PROTECTED `Header.tsx`/`Footer.tsx` and in `B2BSidebar.tsx`; the filename is unchanged so those files are NOT edited.
- Done criteria: `public/images/logo-mobil.png` is ≤ ~30 KB, 144×144, valid PNG; logo still renders at all use sites (max display 48px); build passes.
- Rollback steps: restore the previous binary from git history (`git checkout <prev> -- public/images/logo-mobil.png`).
- Owner: Doğan / Claude
- Date: 2026-06-07

## Notes
- Follow-up debt from PR #298 verification. Filename kept identical to avoid touching PROTECTED Header/Footer.
- `<img>` → `next/image` swap intentionally deferred (would require PROTECTED file edits + approval); asset optimization alone removes the >1 MB perf problem.
