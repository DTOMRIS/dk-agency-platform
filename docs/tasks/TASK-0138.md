# TASK-0138 — Admin: İstifadəçi Detail Səhifəsi

**Status:** done
**Priority:** medium
**Sprint:** 2026-05-17

## Scope
Member detail page: profil kartı + audit tarixçəsi + MembersTable-dan link.

## Files Created
- `app/dashboard/users/[id]/page.tsx` — detail UI (profile card, audit timeline)

## Files Modified
- `app/api/admin/members/[id]/route.ts` — GET method added (profile + last 10 audit logs)
- `components/dashboard/MembersTable.tsx` — "Bax" link per row
- `e2e/admin-members.spec.ts` — GET detail + page smoke tests
- `messages/az.json`, `en.json`, `ru.json`, `tr.json` — memberDetail namespace + view key

## Security
- memberProfiles has no passwordHash (separate users table)
- Named select used — future columns won't auto-leak
- Admin-only auth check (JWT role === 'admin')
