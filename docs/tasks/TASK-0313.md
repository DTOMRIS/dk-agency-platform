# TASK-0313

- ID: `TASK-0313`
- Scope: Site-wide Open Graph + Twitter Card metadata (homepage/all pages via root layout; blog + news detail per-article).
- Reason: TASK-0300 audit — root layout had only title+description (no OG/Twitter), so homepage and every page without its own OG produced no social preview; blog + news had openGraph but no twitter card. Homepage is a client component (`app/[locale]/page.tsx` is 'use client'), so it cannot export generateMetadata — root layout is the correct place for site defaults.
- Affected files:
  - `app/layout.tsx`: added `openGraph` (type website, siteName, locale, url, title, description) + `twitter` (summary_large_image) to root metadata.
  - `app/[locale]/blog/[slug]/page.tsx`: added `twitter` block mirroring openGraph (image from coverImage).
  - `app/haberler/[slug]/page.tsx`: added `twitter` block mirroring openGraph (image from imageUrl).
- Do-not-touch list: homepage component, news/blog repositories, schema — untouched.
- Done criteria: tsc clean; social share of homepage/blog/news shows title+description+image card.
- Rollback steps: revert the three files.
- Follow-up (separate task): branded default OG image (ImageResponse route) for articles without a cover image.
