# TASK-0134 — Admin İstifadəçilər → Real DB

**Status:** done
**Priority:** high
**Sprint:** 2026-05-16

## Scope
Replace mock adminUsers on /dashboard/users with real DB query (memberProfiles + memberSubscriptions).

## Files Created
- `app/api/admin/members/route.ts` (GET, admin-only, JOIN, pagination, stats)
- `components/dashboard/MembersTable.tsx` (filter, pagination, plan badges, 4 dil)
- `e2e/admin-members.spec.ts`

## Files Modified
- `app/dashboard/users/page.tsx` (mock → real fetch, stat cards, MembersTable)

## Auth
- JWT pattern (getAuthFromCookie) — same as dashboard layout
- role === 'admin' required, else 403
- passwordHash NEVER in SELECT or response
