# TASK-0415 — Security dependency sweep

Date: 2026-06-25
Branch: `fix/security-dependency-sweep`

## Scope

Hostinger/GitHub dependency scan reported 74 vulnerabilities, including 1 critical and 30 high. This task applies a controlled dependency sweep without changing application behavior.

## Changes

- Upgraded direct runtime packages:
  - `next` 16.1.6 -> 16.2.9
  - `next-intl` 4.8.3 -> 4.13.0
  - `drizzle-orm` 0.45.1 -> 0.45.2
  - `nodemailer` 8.0.6 -> 9.0.1
  - `@google/genai` 1.42.0 -> 2.10.0
  - `cloudinary` 2.9.0 -> 2.10.0
  - `postcss` 8.5.8 -> 8.5.15
  - `xlsx` 0.18.5 -> SheetJS 0.20.3 official tarball
- Moved `drizzle-kit` from runtime dependencies to `devDependencies`.
- Upgraded `eslint-config-next` to 16.2.9.
- Added npm overrides for vulnerable transitive dependencies:
  - `protobufjs`, `@protobufjs/utf8`, `ws`, `lodash`, `dompurify`
  - `@opentelemetry/*`, `@babel/core`, `esbuild`
  - `flatted`, `picomatch`, `js-yaml`

## Audit result

- Before: 74 total vulnerabilities reported by hosting scan; local prod audit had 1 critical and 7 high.
- After:
  - Full `npm audit`: 4 moderate, 0 high, 0 critical.
  - `npm audit --omit=dev`: 4 moderate, 0 high, 0 critical.
- Remaining moderate findings are the single upstream Next.js bundled PostCSS chain:
  - `next` -> bundled `postcss@8.4.31`
  - audit also reports dependent packages `@sentry/nextjs` and `next-intl`
- `npm audit fix --force` recommends downgrading Next to 9.3.3, which is not a valid fix for this Next 16 app.

## Validation

- `npm run lint` passes with existing warnings.
- `NEXT_TURBOPACK_EXPERIMENTAL_USE_SYSTEM_TLS_CERTS=1 npm run build` passes.
- `npx tsc --noEmit` still fails on pre-existing project type debt; build intentionally skips type validation as configured.

## Protected files

`package.json` and `package-lock.json` were changed with explicit user approval for security remediation.
