# Task Card

- ID: `TASK-0457`
- Scope: fix(security): istənilən şəxs özünü admin edə bilirdi (imzasız member cookie + `POST /api/member/session` bədəndəki `plan`-ı yazırdı); qeydiyyatlı üzv dashboard lead səhifələrini aça bilirdi
- Affected files: lib/members/server-session.ts, app/api/member/session/route.ts, lib/auth/guards.ts (`requireAdminPage`), app/dashboard/{page, blog/[slug], blog/translation-status, contact-tracking, franchise-leads, kazan-leads, xeberler/[id]}/page.tsx (+2 sətir hər biri), e2e/auth-session-security.spec.ts (yeni), e2e/blog-translate-job.spec.ts (auth sadələşdi), docs
- Do-not-touch list: PROTECTED — `lib/member-access.ts`, `app/api/member/auth/route.ts`, `middleware.ts` toxunulmadı; `docs/STATE.md`-yə toxunulmadı
- Done criteria: saxta cookie və bədəndəki `plan:'admin'` admin API vermir; member JWT → admin API 403, lead səhifələri → /b2b-panel; admin JWT → 200; `marketinq-ocagi` üzvə açıq qalır
- Rollback steps: `git revert` (açıq geri qayıdır — yalnız başqa həll ilə)
- Owner: Doğan («başla paşam güvenliği düzelt») / Claude (icra)
- Date: 2026-09-26

## Notes
- Səlahiyyət mənbəyi: imzalı JWT `dk_auth_token` (httpOnly, 7 gün). `dk_member_session`-dan yalnız ad (e-poçt JWT ilə eyni olanda).
- Mövcud admin sessiyası pozulmur: JWT artıq var, sessiya ondan qurulur. JWT-si bitən istifadəçi yenidən giriş edir.
- Layout admin-only edilmədi: `app/b2b-panel/analizler` üzvü `/dashboard/marketinq-ocagi/lokasyon-analiz`-ə göndərir. Client səhifələri (users, faturalar, ...) datanı guard-lı API-dən alır — üzv 403 alır. Tam həll (dashboard-u admin/üzv hissəyə bölmək) TD-004-də.
- `/api/member/auth` (PROTECTED) cookie yazır, amma artıq səlahiyyət mənbəyi deyil — toxunmağa ehtiyac qalmadı.
