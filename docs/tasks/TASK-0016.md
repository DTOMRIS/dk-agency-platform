# TASK-0016

- Scope: Invoice export (CSV/Excel) + category admin UI
- Affected files:
  - lib/invoice-ocr/export-utils.ts (CSV + Excel export with UTF-8 BOM)
  - app/dashboard/faturalar/page.tsx (export buttons wired up)
  - app/dashboard/fatura-kateqoriyalar/page.tsx (NEW — category CRUD UI)
- Do-not-touch list:
  - DB schema (no changes)
  - auth flows
  - middleware.ts
