# TASK-0312

- ID: `TASK-0312`
- Scope: News RSS feed (`/api/rss/haberler`) now serves real approved news from the DB instead of blog mock data.
- Reason: TASK-0300 audit finding — the feed titled "DK Agency Xəbərlər" called `getAllBlogArticles()` (static blog mock) and emitted `/haberler/<blog-slug>` links that 404, since blog slugs don't resolve under the news route.
- Affected files:
  - `app/api/rss/haberler/route.ts`: `getAllBlogArticles()` → `getApprovedNewsArticles({ limit: 50 })`; GET is now async + `dynamic = 'force-dynamic'`; item fields mapped from `PublicNewsArticle` (slug/title/summary/publishedAt).
- Do-not-touch list: news repository, schema, admin routes (unchanged).
- Done criteria: feed lists real approved news; links resolve to live `/haberler/<news-slug>`; tsc clean.
- Rollback steps: revert the single route file.
