# Task Card

- ID: `TASK-0213`
- Scope: Fix KAZAN AI mobile widget overflow + make the AI call resilient so a DeepSeek/Claude network failure or timeout degrades to the useful static answer instead of a hard 500 ("canlı cavab gecikir" error fallback).
- Affected files:
  - `components/kazan-ai/FloatingKazanWidget.tsx` (mobile panel height `100vh` → `100dvh`)
  - `app/api/kazan-ai/route.ts` (added `fetchWithTimeout` + try/catch around DeepSeek & Anthropic fetch → return ok:false on throw/timeout instead of bubbling a 500)
  - `docs/CHANGELOG.md`
- Do-not-touch list: all PROTECTED files. AI provider strategy unchanged (DeepSeek primary + Claude fallback).
- Done criteria: build 0 error; widget uses `100dvh` (fits mobile visible viewport); a thrown/timed-out provider fetch returns ok:false → route falls through to static 200 (no 500 → client no longer shows "canlı cavab gecikir" on a transient provider failure).
- Rollback steps: `git revert` the commit.
- Owner: Doğan / Claude
- Date: 2026-06-07

## Notes / diagnosis
- Screenshot (IMG_3855) showed KAZAN replying with `kazanWidget.errorFallback` ("Hazırda canlı cavab gecikir...") = the client catch message → `/api/kazan-ai` was returning a 500.
- Root cause: `callDeepSeekWithPrompt`/`callAnthropicWithPrompt` did `await fetch(...)` with NO try/catch; if the provider is unreachable/slow from Hostinger the fetch throws → outer catch → 500. (If the key were merely missing, the route would return static 200 instead.)
- ⚠️ This makes failures graceful, but REAL AI answers still require fixing the provider reachability on Hostinger: verify `DEEPSEEK_API_KEY` is set/valid AND that Hostinger can reach api.deepseek.com (check runtime log for the exact error: `fetch failed`/`ETIMEDOUT`/`ENOTFOUND`/`AbortError`). The 30s AbortController timeout also prevents the request from hanging.
- Authenticated/live AI not smoke-tested (sandbox blocks DeepSeek + has no keys); build + logic verified.
