# Task Card

- ID: `TASK-0235`
- Scope: Launch sprint block 2/9 — add the light-zone sector selector with five localized cards, a featured restaurant card, hover feedback, and valid locale-aware destinations.
- Affected files:
  - `components/home/SectorSelector.tsx`
  - `app/[locale]/page.tsx`
  - `messages/{az,en,ru,tr}.json`
  - `docs/CHANGELOG.md`
  - `docs/DEVLOG.md`
- Do-not-touch list: Header, Hero, auth/member session logic, protected config and layout files.
- Done criteria: five cards render after Hero; restaurant is highlighted; hover/focus reveals the explore action; mobile uses two columns; all four locales resolve; links avoid unsupported sector slugs; build and route smoke checks pass.
- Rollback steps: `git revert` the TASK-0235 commit.
- Owner: Doğan / Codex
- Date: 2026-06-07

## Notes

- Dedicated sector landing configs exist for `restoran`, `kafe`, and `otel`.
- `fast-food` and `catering` therefore route to locale-aware `/toolkit?sector=...` destinations instead of unsupported `/sektor/...` pages.
- This branch is based on TASK-0234 to preserve the requested merge/review order; it must not be merged before TASK-0234.
