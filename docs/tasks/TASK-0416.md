# TASK-0416 - Locale dashboard funnel route

- Scope: Fix `/tr/dashboard/funnel` returning 404 while root `/dashboard/funnel` exists.
- Root cause: `app/dashboard/funnel/page.tsx` existed without the matching locale mirror under `app/[locale]/dashboard/funnel/page.tsx`.
- Change: Add locale dashboard funnel page that re-exports the existing root dashboard funnel page.
- Verification:
  - Live before fix: `https://dkagency.com.tr/tr/dashboard/funnel` returned 404.
  - Live root route: `https://dkagency.com.tr/dashboard/funnel` returned 307 to `/auth/login`.
  - Local production build lists `ƒ /[locale]/dashboard/funnel`.
  - Local built route `http://127.0.0.1:3001/tr/dashboard/funnel` returns 307 to `/auth/login`, not 404.
