# TASK-0315

- ID: `TASK-0315`
- Scope: Banner ad system PR-1 — DB table + admin CRUD (schema, idempotent migration, repository, admin API, admin page + sidebar nav). Public AdSlot rendering + impression/click tracking = PR-2.
- Reason: Owner-requested advertising system. The existing `AdsPreview` is a listings board (mock `AD_ITEMS`), not an ad system — banner ads were greenfield.
- Affected files:
  - `lib/db/schema.ts`: NEW `ads` table (format text image|gif|video, mediaUrl, targetUrl, placement, schedule, impressions/clicks, isActive). Migration-backed per PROTECTED.md.
  - `drizzle/0019_add_ads.sql`: idempotent CREATE TABLE + index. **Owner runs this in Neon (production branch) — drizzle-kit does not auto-run on Hostinger.**
  - `lib/repositories/adsRepository.ts`: list/get/create/update/delete + getActiveAdsByPlacement (schedule-aware) + impression/click increments. AD_FORMATS / AD_PLACEMENTS SSOT.
  - `app/api/admin/ads/route.ts` (GET list, POST create) + `app/api/admin/ads/[id]/route.ts` (PATCH, DELETE) — admin-session gated (plan==='admin'), Zod-validated.
  - `app/dashboard/reklamlar/page.tsx` + `app/[locale]/dashboard/reklamlar/page.tsx` mirror (L-038).
  - `components/dashboard/DashboardSidebar.tsx`: nav item (Megaphone) — page wired, not orphaned.
  - `messages/{az,ru,en,tr}.json`: `dashboardSidebar.ads` (4-language parity).
- Do-not-touch list: next.config.ts (CSP) — html5/lottie formats deferred to a later PR that needs CSP `frame-src`/`media-src`. image/gif/video work today (Cloudinary under existing img-src).
- Done criteria: migration run in Neon; admin can create/toggle/delete an ad; GET/POST/PATCH/DELETE return 403 without admin session. tsc + lint clean.
- Rollback steps: drop `ads` table; revert the listed files.
- Follow-up: PR-2 = `<AdSlot placement>` public render + `/api/ads/[id]/impression|click` tracking + place slots. PR-3 = CSP + html5/lottie.
