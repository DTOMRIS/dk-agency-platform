# Task Card

- ID: `TASK-0446`
- Scope: fix(ci): «Daily News Fetch» cron-u `.env.local` olmadığı üçün exit 9 ilə sınırdı — `--env-file` → `--env-file-if-exists` (4 script), workflow Node 20 → 22 (engines `>=22`)
- Affected files: package.json (PROTECTED, ALLOW_PROTECTED=1, sahib «hata var düzelt»), .github/workflows/fetch-news.yml, docs/LESSONS.md (L-050), docs/CHANGELOG.md, docs/DEVLOG.md
- Do-not-touch list: PROTECTED.md — yalnız package.json 4 sətir (script bölməsi); `.env*`-ə yazılmadı; `--no-verify` işlədilmədi
- Done criteria: `npm run fetch:news -- --limit=1 --no-translate` `.env.local` olmadan exit 0; workflow `workflow_dispatch` ilə yaşıl (merge-dən sonra sahib/CI)
- Rollback steps: `git revert` (PR merge commit-i); DB/data dəyişikliyi yoxdur
- Owner: Doğan (qərar) / Claude (icra)
- Date: 2026-09-13

## Notes
- Son 100 cron icrasının hamısı FAIL (ən azı 2026-06-06-dan); `--env-file=.env.local` PR #394 (2026-06-14) ilə gəlib — `node --env-file` fayl yoxdursa sərt dayanır (exit 9), CI-da `.env.local` heç vaxt olmur, sirlər `env:` ilə gəlir.
- `--env-file-if-missing` deyə bayraq YOXDUR (Node 22.22 «bad option»); düzgün ad `--env-file-if-exists`.
- Tam detal: `docs/CHANGELOG.md` və `docs/DEVLOG.md` (TASK-0446 girişi)
