# TASK-0031

- Scope: Replace `request.nextUrl.origin` and hardcoded `process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3000'` patterns in auth routes with `getBaseUrl()` to produce absolute redirect and email link URLs that point to the public domain behind Hostinger's reverse proxy.
- Affected files:
  - app/api/auth/confirm/route.ts
  - app/api/auth/register/route.ts
  - app/api/auth/route.ts
- Do-not-touch list:
  - DB schema
  - Auth JWT logic
- Done criteria:
  - All redirect() and email link calls use `getBaseUrl()` instead of `request.nextUrl.origin` or raw env vars
  - Confirm email links resolve to https://dkagency.com.tr/...
  - Password reset links resolve to https://dkagency.com.tr/...
- Rollback steps:
  1. Revert the three auth route files to their previous content
- Owner: Dogan
- Date: 2026-05-04
