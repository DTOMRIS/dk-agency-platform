# TASK-0263

- Scope: News content display fix, news CRUD completion, listings detail page, free listing limit
- Reason: 12-agent audit revealed 8 critical bugs: news content not rendering (SELECT missing contentAz/Ru/En/Tr), no DELETE endpoint for news or listings, PATCH limited to 5 fields, preview 404 for draft articles, listings missing detail page (only modal), no spam protection for free listings.
- Affected:
  - `lib/repositories/newsRepository.ts`: Added contentAz/Ru/En/Tr to getNewsArticleBySlug SELECT, added preview mode parameter, expanded updateNewsArticleAdmin to 22 fields, added deleteNewsArticle function, expanded getAdminNewsArticleById to return all fields.
  - `app/api/news/admin/[id]/route.ts`: Added DELETE and GET handlers, expanded PATCH to accept all content/SEO/vitrine fields.
  - `app/haberler/[slug]/page.tsx`: Added ?preview=true support for draft article preview.
  - `components/dashboard/NewsEditorForm.tsx`: Updated preview link to include ?preview=true query param.
  - `app/[locale]/ilanlar/[slug]/page.tsx`: NEW — SSR listing detail page with SEO metadata, OG tags, shareable URL.
  - `components/listings/ListingDetailClient.tsx`: NEW — Client component for listing detail (reuses ListingModal layout).
  - `lib/db/listings-repository.ts`: Added getListingBySlug function.
  - `app/api/listings/[id]/route.ts`: Added DELETE handler (admin only), admin PATCH bypass for any status.
  - `app/api/listings/route.ts`: Added 2 free listing limit per non-admin user (MAX_FREE_LISTINGS=2).
  - `app/dashboard/ilan-onaylari/page.tsx`: Replaced mock page with redirect to /dashboard/ilanlar.
- DoD: News content renders on detail page, draft preview works, admin can delete/edit news and listings, listing detail page has SEO URL, non-admin users limited to 2 free listings.
