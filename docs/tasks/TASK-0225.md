# Task Card

- ID: `TASK-0225` (P1)
- Scope: Auto-translate a blog post's AZ master into ru/en/tr on publish, so new content no longer stays AZ-only until a manual `npm run content:translate` run.
- Affected files:
  - `lib/ai/translate.ts` (new — server-side DeepSeek `translateText`, 60s timeout, brand-name guard, best-effort/null-on-fail)
  - `lib/db/blog-repository.ts` (`autoTranslateBlogPost(id)` — fills only EMPTY ru/en/tr title/summary/content from AZ; never overwrites manual translations; never throws)
  - `app/api/blog/route.ts` (POST fires `autoTranslateBlogPost` fire-and-forget when status=published)
- Do-not-touch list: PROTECTED files untouched.
- Done criteria: build 0 error; on publish, missing locale fields fill in the background; failure leaves AZ (no silent corruption).
- HALT note (resolved): the serverless-10s-timeout concern does NOT apply — DK runs on Hostinger (long-running Node), so fire-and-forget background translation completes safely without a queue.
- Verification: build PASS. Live DeepSeek call not exercised in sandbox (DEEPSEEK_API_KEY/outbound blocked); logic + graceful-failure verified.
- Rollback: `git revert`.
- Owner: Doğan / Claude · Date: 2026-06-07

## Notes
- Only EMPTY fields are filled → admin's manual translations are respected.
- Follow-up: wire the same call into the blog PATCH (edit→publish) route and into the news pipeline (PR-6).
