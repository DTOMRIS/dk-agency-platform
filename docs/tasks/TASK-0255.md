# TASK-0255: Translate root cause fix

## Scope
- `lib/ai/translate.ts` — retry 3x, chunking, dynamic tokens, error logging
- `lib/db/blog-repository.ts` — needsTranslation AZ char detection, per-lang UPDATE
- `app/[locale]/blog/page.tsx` — locale-aware link

## Reason
title_ru = AZ text (DeepSeek copied, not translated). needsTranslation missed it
(case-insensitive match). No retry, no log, 8K token limit too small.

## Affected
- Blog translation pipeline
- Blog list page navigation
- All 21 blog posts with partial translations

## DoD
- [x] Build passes
- [x] needsTranslation detects AZ chars in RU/EN fields
- [x] translateText retries 3x with backoff
- [x] Blog list links include locale
- [ ] franchise-bedelleri title_ru = Russian (post-deploy verify)
