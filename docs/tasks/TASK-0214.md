# Task Card

- ID: `TASK-0214`
- Scope: Replace the default Next.js favicon (the "triangle" users read as a Vercel mark in the browser tab) with the DK Agency logo, and add proper PWA / home-screen icons so the mobile "Add to Home Screen" uses the DK icon.
- Affected files:
  - `app/favicon.ico` (DELETED — was the default Next.js favicon, 25931 bytes)
  - `app/icon.png` (new — 256, Next.js favicon convention)
  - `app/apple-icon.png` (new — 180, iOS home-screen icon)
  - `public/icon-192.png`, `public/icon-512.png` (new — PWA manifest icons)
  - `app/manifest.ts` (icons now reference the PNG icons + maskable)
  - `docs/CHANGELOG.md`
- Do-not-touch list: all PROTECTED files. NOTE: used Next.js file conventions (app/icon.png, app/apple-icon.png) so `app/layout.tsx` was NOT edited.
- Done criteria: build emits `/icon.png`, `/apple-icon.png`, `/manifest.webmanifest`; browser tab shows DK logo (not the default triangle); manifest references 192/512 icons.
- Rollback steps: `git revert` the commit (restores app/favicon.ico).
- Owner: Doğan / Claude
- Date: 2026-06-07

## Notes
- Icons generated from the original 1254×1254 logo recovered from git (commit 33206b1), via sharp.
- The "Vercel mark" was the default Next.js favicon.ico (exact 25931-byte default). No Vercel code exists in the repo.
- ⚠️ If a floating Vercel *toolbar* still appears on a deployment, that is a Vercel project setting (Toolbar), not code.
- Auto "Add to Home Screen" banner on Android also needs a service worker (separate task); iOS manual Add-to-Home-Screen now shows the DK icon.
