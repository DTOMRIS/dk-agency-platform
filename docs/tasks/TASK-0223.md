# Task Card

- ID: `TASK-0223` (SEC-P0)
- Scope: Close the P0 IDOR / missing-auth vulnerability on `/api/invoices`. The endpoints accepted a `userId` from the request and had NO session/auth check — anyone could read any user's invoices (GET `?userId=`), create invoices, or **delete ANY invoice by id** (DELETE).
- Affected files:
  - `app/api/invoices/route.ts` (GET/POST/DELETE require a logged-in session; userId derived from session email, never from the request)
  - `lib/repositories/invoiceRepository.ts` (`bulkDeleteInvoices(ids, userId)` — ownership-scoped delete)
- Do-not-touch list: PROTECTED — `lib/member-access.ts`, `middleware.ts` (read only). Used existing `getServerMemberSession` + email→userId pattern (same as `/api/user/events`).
- Done criteria: unauth request → 401 on GET/POST/DELETE/stats; request `userId` is ignored (forced from session); delete only removes the caller's own invoices; build 0 error.
- Verification (done): build PASS; smoke — GET / `?userId=5` / stats / POST / DELETE all → **401** without session (previously wide open).
- Rollback steps: `git revert`.
- Owner: Doğan / Claude
- Date: 2026-06-07

## Notes
- userId resolution: `resolveUserId(session.email)` → `users.id`.
- Session check runs BEFORE the dbAvailable early-return, so unauth → 401 even without DB (sandbox-verifiable).
- Admin currently sees only their own invoices (owner-scoped) — security fix, not a regression (was previously fully open). A future explicit admin-all override can be added.
- Follow-up: auth pass on sibling routes `invoice-ocr` / `invoice-pdf`.
