# Task Card

- ID: `TASK-0216` (F-RADAR Faza 1)
- Scope: Franchise Radar — curated directory of franchise brands not yet in Azerbaijan, with a sector filter and per-brand lead funnel. Field-by-field data layer (Faza-2 DB ready). Reuses sektorConfigs SSOT + ota-guide lead pattern (L-009).
- Affected files:
  - `lib/data/franchiseDirectory/{types,brands,index}.ts` (SSOT, 8 pilot brands, real open-source data)
  - `app/[locale]/franchise/radar/page.tsx` + `app/franchise/radar/page.tsx` (root mirror — L-038)
  - `components/franchise/FranchiseRadarCatalog.tsx` + `FranchiseLeadModal.tsx`
  - `app/api/lead/franchise-radar/route.ts` (lead funnel)
  - `lib/analytics/franchiseEvents.ts` (view/filter/lead)
  - `messages/{az,ru,en,tr}.json` (franchiseRadar namespace, 4 langs)
  - `docs/LESSONS.md` (L-039), `docs/CHANGELOG.md`, this card
- Do-not-touch list: PROTECTED — `lib/db/schema.ts` (enum reused, not extended), marketing-tools-config, listingFieldConfig, member-access, middleware, Header, Footer, layout, next.config, package.json, tailwind.config.
- Done criteria: build 0 error; `/franchise/radar` (az) 200, `/az/...` 307, `/en|/ru` 200, unknown 404; lead API wired (503 w/o DB in sandbox → 400 validation in prod); no hosted brand logos (icons only) + disclaimer present; i18n 4 langs no MISSING_MESSAGE.
- Rollback steps: `git revert`; delete `lib/data/franchiseDirectory/`, `components/franchise/Franchise*`, the radar routes, the lead route, franchiseEvents.
- Owner: Doğan (CTO spec) / Claude
- Date: 2026-06-07

## Verification (done)
- Build PASS; routes smoke 200/307/200/200/404; lead POST {} → 503 (no DB in sandbox).
- L-039 legal lesson recorded (no logo hosting; enum reuse + score jsonb).

## Next (NOT this sprint)
- Faza 2: brand-owner self-service portal (field-by-field form + Cloudinary + moderation), after Devir M5.3b/M5.4, same moderation pattern.
- TR brands expansion (Gönül Kahvesi investment data) from Doğan.
