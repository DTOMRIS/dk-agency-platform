# TASK-0030

- Scope: Add `experimental.trustHostHeader: true` to next.config.ts so Next.js standalone server respects the `X-Forwarded-Host` header sent by Hostinger's reverse proxy when generating redirect URLs.
- Affected files:
  - next.config.ts
- Do-not-touch list:
  - All other config files
  - App business logic
- Done criteria:
  - next.config.ts contains `experimental: { trustHostHeader: true }`
  - Auth redirects use the public hostname instead of the internal 0.0.0.0 address
- Rollback steps:
  1. Remove the `experimental.trustHostHeader` key from next.config.ts
- Owner: Dogan
- Date: 2026-05-04
