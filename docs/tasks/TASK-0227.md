# Task Card

- ID: `TASK-0227` (P1)
- Scope: KAZAN Leads → real CRM. Add notes timeline, lead scoring, and next-contact reminder so the leads panel is usable (not just a static list). Builds on migration 0008 (notes/lead_score/next_contact_at, run by Doğan on Neon).
- Affected files:
  - `lib/repositories/kazanLeadRepository.ts` (UpdateKazanLeadInput + updateKazanLead: append note to `notes` jsonb, set leadScore/nextContactAt; `computeLeadScore` heuristic)
  - `app/api/kazan-ai/leads/route.ts` (PATCH accepts addNote/leadScore/nextContactAt)
  - `components/dashboard/KazanLeadStatusActions.tsx` (score badge, next-contact badge, notes timeline, add-note + reminder-date panel)
  - `app/dashboard/kazan-leads/page.tsx` (passes notes/leadScore/nextContactAt)
  - `messages/{az,ru,en,tr}.json` (kazanLeadActions: noteToggle/noNotes/notePlaceholder/nextContact/saveNote)
- Do-not-touch list: PROTECTED. schema.ts columns added by Doğan (approved); not edited here.
- Done criteria: build 0 error; notes append (never overwrite); score/reminder persist; i18n 4 langs (no MISSING_MESSAGE).
- Verification: build PASS; 4-lang key completeness PASS. Live PATCH not exercised (sandbox no DB).
- Rollback: `git revert`.
- Owner: Doğan / Claude · Date: 2026-06-07

## Notes / follow-up
- computeLeadScore is a heuristic (intent + status + whatsapp/meeting). Auto-apply on status change can be wired later.
- Follow-up email template (send from a lead) — proposed next small step.
- Kanban board view — optional polish.
