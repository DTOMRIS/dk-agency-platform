# TASK-0055

- ID: `TASK-0055`
- Scope: News single source of truth — remove mock DB, redirect haberler to sektor-nebzi
- Affected files:
  - `app/api/news/route.ts` — replaced mockNewsDB with real DB via getApprovedNewsArticles
  - `app/sektor-nebzi/page.tsx` — NEW canonical news listing page
  - `app/sektor-nebzi/[slug]/page.tsx` — NEW canonical news detail page
  - `app/[locale]/sektor-nebzi/page.tsx` — NEW localized re-export
  - `app/[locale]/sektor-nebzi/[slug]/page.tsx` — NEW localized re-export
  - `app/haberler/page.tsx` — replaced with redirect to /sektor-nebzi
  - `app/haberler/[slug]/page.tsx` — replaced with redirect to /sektor-nebzi/[slug]
  - `app/[locale]/haberler/page.tsx` — replaced with redirect to /[locale]/sektor-nebzi
  - `app/[locale]/haberler/[slug]/page.tsx` — replaced with redirect to /[locale]/sektor-nebzi/[slug]
  - `app/dashboard/haberler/page.tsx` — replaced with redirect to /dashboard/xeberler
  - `app/sitemap.ts` — replaced /haberler entries with /sektor-nebzi
  - `lib/repositories/newsRepository.ts` — removed console.log production calls
- Do-not-touch list: `lib/db/schema.ts`, `middleware.ts`, `package.json`
- Done criteria:
  - [x] /api/news uses real DB (getApprovedNewsArticles), not mockNewsDB
  - [x] /sektor-nebzi is the canonical news route with all correct internal links
  - [x] /haberler, /[locale]/haberler redirect to /sektor-nebzi equivalents (301)
  - [x] sitemap.ts no longer references /haberler/* paths
  - [x] dashboard/haberler redirects to dashboard/xeberler
  - [x] No console.log in production paths
  - [x] No `any` types used
  - [x] Build passes
- Rollback steps:
  - git revert or git checkout main on affected files
  - mockNewsDB.ts remains untouched (still importable from lib/data/mockNewsDB)
- Owner: Builder Agent
- Date: 2026-05-04

## Notes
- `lib/data/mockNewsDB.ts` is NOT deleted — it is still imported by `newsRepository.ts`
  getAdminNewsArticles() mock fallback. Deleting it would break the DB-unavailable path.
- `components/news/SektorNabziTabs.tsx` and `components/editorial/HaberlerPageClient.tsx`
  were reviewed but not changed — they are used by different sub-routes (sektor-nebzi blog tabs)
  and do not serve the canonical public news feed.
- The /api/news endpoint now accepts `locale` query param for locale-aware response.
- Pagination: limit (max 100), offset are both supported with safe bounds.
