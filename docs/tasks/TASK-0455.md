# Task Card

- ID: `TASK-0455`
- Scope: fix(blog): «🌐 Avtomatik tərcümə» işləmirdi — məcburi yenidən tərcümə, arxa plan işi + vəziyyət sorğusu, parçalama/paralel sürət, yaradılışda avtomatik tərcümə heç işə düşmürdü
- Affected files: lib/ai/translate.ts (parçalama + paralel), lib/db/blog-repository.ts (`force` seçimi), lib/blog/translationJobs.ts (yeni, iş reyestri), app/api/blog/translate/route.ts (POST 202 + GET vəziyyət), app/api/blog/route.ts (yaradılışda tərcümə), components/dashboard/BlogEditorForm.tsx (təsdiq, sorğu, state yeniləmə), e2e/blog-translate-chunking.test.ts (yeni), e2e/blog-translate-job.spec.ts (yeni), docs
- Do-not-touch list: PROTECTED.md — toxunulmadı; auth faylları toxunulmadı; `docs/STATE.md`-yə toxunulmadı; DB sxemi dəyişmədi
- Done criteria: düymə mövcud tərcümənin üstünə yazır (təsdiqlə); sorğu dərhal qayıdır, redaktor vəziyyəti göstərir və bitən tərcüməni state-ə yazır; uzun mətn ≤3000 simvolluq parçalarla paralel; ``` bloku bölünmür; yeni yazı (qaralama da) avtomatik tərcümə olunur
- Rollback steps: `git revert`
- Owner: Doğan («tercüme neden işlemiyor, deep seek var») / Claude (icra)
- Date: 2026-09-26

## Notes — tapılan 5 kök səbəb
1. `needsTranslation` yalnız boş və ya AZ hərfli sahəni tərcümə edir → AZ mətni dəyişəndə köhnə RU/EN/TR «tərcümə olunub» sayılır, düymə «RU — · EN — · TR —» göstərib heç nə etmir.
2. >6000 simvol mətn hər `##`-də ayrı çağırışa bölünürdü, ardıcıl (nümunədə 15 çağırış) → 1–3 dəq açıq sorğu → proxy kəsir → «Tərcümə xidməti əlçatmadı».
3. Köhnə bölmə ``` bloku içindəki `#` sətrini də başlıq sayırdı — kod bloku iki çağırışa bölünürdü.
4. Yaradılışda avtomatik tərcümə `created.id`-yə baxırdı, amma `createBlogPostInDb` yalnız `{ slug, source }` qaytarır → heç vaxt işə düşmürdü.
5. O da yalnız `status === 'published'` üçün idi, redaktor isə yeni yazını default «draft» saxlayır.
- Əlavə: tərcümədən sonra `router.refresh()` redaktorun state-ini yeniləmirdi (`useState` ilkin dəyəri) → növbəti «Yadda saxla» köhnə RU/EN/TR-i geri yazardı. İndi GET yeni sahələri qaytarır, state-ə yazılır.
- Yolda tapılan (bu PR-da DEYİL, ayrıca təhlükəsizlik task-ı): `dk_member_session` imzasız base64 JSON-dur və `POST /api/member/session` bədəndəki `plan: 'admin'`-i yoxlamadan cookie-yə yazır → istənilən şəxs admin API-larına çıxış ala bilər. Lokal dev-də təsdiqləndi.
