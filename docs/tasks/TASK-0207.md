# Task Card

- ID: `TASK-0207`
- Scope: Claude memory infrastructure — step 1/3: SessionStart "brain" injection so every new session starts with full project knowledge instead of from zero.
- Affected files:
  - `.claude/memory/CLAUDE-BRAIN.md` (new — curated rules/invariants/deploy-reality/traps)
  - `scripts/session-brain.mjs` (new — injects brain + live git state at SessionStart)
  - `.claude/settings.json` (SessionStart hook → node script)
  - `docs/CHANGELOG.md`
- Do-not-touch list: all PROTECTED files (member-access, listingFieldConfig, marketing-tools-config, middleware, Header, Footer, layout, next.config, package.json, tailwind.config), `.env*`
- Done criteria: `node scripts/session-brain.mjs` emits valid JSON with `additionalContext` containing the brain + live branch/commits; hook wired in settings.json.
- Rollback steps: revert `.claude/settings.json` SessionStart to echo command; delete new files.
- Owner: Doğan / Claude
- Date: 2026-06-07

## Notes
- Part of the 3-step memory/skill infrastructure (brain → auto-journal → skills).
