# TASK-0139 — Admin: İstifadəçi Şifrəsini Sıfırla + Email

**Status:** done
**Priority:** medium
**Sprint:** 2026-05-17

## Scope
Admin detail page-dən istifadəçiyə şifrə sıfırlama emaili göndərə bilir.
Admin heç vaxt şifrəni görmür — user özü yeni şifrəni seçir.

## Files Created
- `app/api/admin/members/[id]/reset-password/route.ts` — POST endpoint

## Files Modified
- `app/dashboard/users/[id]/page.tsx` — QuickActions block with reset button
- `lib/email/templates.ts` — adminPasswordReset template (4 languages)
- `e2e/admin-members.spec.ts` — POST reset-password 401 test
- `messages/az.json`, `en.json`, `ru.json`, `tr.json` — memberDetail.reset* keys

## Token Mechanism
- Reuses existing `passwordResetTokens` table + `/reset-password` page
- Token: crypto.randomUUID(), 1 hour expire
- Same consume logic as forgot-password (existing endpoint)
- Zero new pages created

## Security
- Admin never sees the password
- Token/hash never written to audit log metadata
- Audit action: member.password_reset, metadata: { initiatedBy: 'admin' }
- UI: window.confirm before sending (prevent accidental clicks)
