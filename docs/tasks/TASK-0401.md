# TASK-0401 - NewsData.io discovery + scoring + draft insert

- ID: `TASK-0401`
- Scope: Fetch news from NewsData.io API, score by relevance (HoReCa/franchise/tourism keywords + source quality), deduplicate by URL hash, insert qualifying articles as `status='fetched', origin='newsdata'` drafts.
- Affected files:
  - `lib/news/newsdata-fetch.ts` (new — API client + fetch + score + insert)
  - `lib/news/scoring-config.ts` (new — SSOT for keyword/source weights + threshold)
  - `scripts/newsdata-fetch.ts` (new — CLI script for `npm run news:fetch`)
  - `lib/db/schema.ts` — add `origin`, `sourceUrlHash`, `relevanceScore` columns
  - `drizzle/0018_add_news_origin_columns.sql` — idempotent migration
  - `package.json` — add `news:fetch` script
- NOT touched: existing RSS pipeline, match-toolkits, translate, admin UI, cron
- Prerequisites: `NEWSDATA_API_KEY` env var, migration 0018 run on Neon
- Done criteria:
  - `npm run news:fetch` fetches from NewsData.io and inserts qualified articles
  - Re-run produces 0 duplicates (hash guard)
  - Articles below score threshold are discarded (not inserted)
  - Build + lint 0 errors
