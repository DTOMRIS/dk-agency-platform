# TASK-0135 — Admin: İstifadəçiyə Rol Ver/Al

**Status:** done
**Priority:** high
**Sprint:** 2026-05-17

## Scope
Admin panel: istifadəçi rolunu UI-dan dəyişdirmək (member ↔ admin). Öz rolunu dəyişmək qadağandır.

## Files Created
- `app/api/admin/members/[id]/route.ts` (PATCH — role update, self-protection)

## Files Modified
- `app/api/admin/members/route.ts` (currentUserId response-a əlavə edildi)
- `app/dashboard/users/page.tsx` (currentUserId state + handleRoleChange callback)
- `components/dashboard/MembersTable.tsx` (rol sütunu: select dropdown + self disabled)
- `e2e/admin-members.spec.ts` (PATCH 401/400 smoke tests)
- `messages/az.json`, `messages/en.json`, `messages/ru.json`, `messages/tr.json` (roles.* i18n)

## Auth
- JWT pattern (getAuthFromCookie) — admin-only
- Self-role protection: targetId === auth.userId → 403
- Valid roles: "member" | "admin"

## Error Statuses
- 400: invalid role, invalid id, invalid body
- 401: not authenticated
- 403: not admin / self-role change
- 404: user not found
- 503: database unavailable
