# TASK-0045: feat(i18n): translate dashboard sidebar (4 languages)

## Status: completed
## Branch: feat/i18n-phase-1-core
## Date: 2026-05-04

## Description
Localize `components/dashboard/DashboardSidebar.tsx` using Pattern A.
Locale is read from `usePathname()` (first path segment) via `normalizeLocale()` — consistent
with how Header.tsx already works. No locale prop needed.

## Architecture change
The previous `navItems` constant array (with `title` hardcoded as AZ strings) was replaced with
`navItemDefs` using a `titleKey` string. `sidebarItems` (derived in `useMemo`) now resolves
`title` from `copy.nav[item.titleKey]` for the active locale.

## Strings translated (per locale: az / en / tr / ru)
- Panel title ("OCAQ İdarə Paneli")
- Admin subtitle ("Admin mərkəzi")
- User access label ("Tam idarəetmə girişi aktivdir")
- Close sidebar aria-label
- Logout button
- All 13 navigation item titles:
  Əsas səhifə / Elanlar / Hero / Xəbərlər / Bloq / KAZAN Leads /
  Auditor / Faturalar / Food Cost / Toolkit / Sayt / İstifadəçilər / Parametrlər

## Files changed
- `components/dashboard/DashboardSidebar.tsx`
