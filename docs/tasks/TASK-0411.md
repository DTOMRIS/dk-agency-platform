# TASK-0411 — basabas branch-opening: number format + EBITDA band + runway fix

- ID: `TASK-0411`
- Status: ✅ Tamamlandı (kod + doğrulama)
- Tarix: 19 İyun 2026

## Scope
Canlıya çıxan branch-opening motorunda (TASK-0409) istifadəçi geri bildirimi əsasında 3 düzəliş — heç biri yeni feature deyil, mövcud davranışın düzəldilməsi.

## Düzəlişlər
1. **Rəqəm formatı** — bütün manat dəyərləri locale-aware minlik ayraçla göstərilir (`fmt0` helper, `Intl.NumberFormat(locale)`): az `239.804`, ru `239 804`, en `239,804`, tr `239.804`. 24 `.toFixed(0)` → `fmt0(...)` (səhifə + endirilən HTML hesabat). Faizlər (`toFixed(1)`) toxunulmadı.
2. **EBITDA benchmark band bug** — yaşıl bandın yuxarı həddi `30` idi; %49 kimi sağlam EBITDA marjı banda düşmür, fallback `red` qaytarırdı (ekranda "Zəif" göründü). `max: 30 → 1000`. İndi %49 → yaşıl/"Güclü".
3. **Mənfi runway** — `runwayMonths` `Math.max(0, …)` ilə qorunmurdu; büdcə açılış investisiyasından az olanda "-3.4 ay" göstərirdi. `cashRunwayMonths` ilə eyni cür sıfıra sabitləndi.

## Dəyişən dosyalar
- `app/toolkit/basabas/page.tsx` — fmt0 helper + 24 manat dəyəri
- `lib/financial/azerbaijan-tax-config.ts` — EBITDA yaşıl band max 30→1000
- `lib/financial/branch-opening-model.ts` — runwayMonths floor 0

## Verification
- Yeni dəyişikliklər: **0 TS / 0 lint error**
- Deterministik test: EBITDA %49.1→yaşıl, %10→amber, %5→red; format 4 dildə doğru ayraç
- Mobil: `ToolkitStudioLayout` `lg:grid-cols-[1fr_380px]` (<1024px stack), input grid-ləri `sm:` ilə <640px tək kolon — yatay daşma yox
