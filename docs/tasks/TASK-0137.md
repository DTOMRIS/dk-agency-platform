# TASK-0137 — Admin: Audit Log (Kim nə etdi, nə vaxt)

**Status:** done
**Priority:** high
**Sprint:** 2026-05-17

## Scope
Admin panel audit log: hər admin əməliyyatını izləyən immutable DB log + API + UI.

## OWASP 2025 Compliance
- Timestamp UTC (with timezone)
- Admin identity (id + email from JWT)
- Target identity (id + email)
- Metadata jsonb (context without credentials)
- Credentials NEVER logged (password, token, hash)
- Immutable: no DELETE endpoint or UI button

## Files Created
- `lib/audit.ts` — writeAuditLog() fire-and-forget utility
- `app/api/admin/audit-logs/route.ts` — GET with pagination + filters
- `app/dashboard/audit-logs/page.tsx` — UI page (table, filters, badges)
- `drizzle/0003_lean_shooting_star.sql` — migration

## Files Modified
- `lib/db/schema.ts` — adminAuditLogs table definition + indexes
- `app/api/admin/members/[id]/route.ts` — audit write on role change
- `app/api/admin/members/route.ts` — audit write on member create
- `components/dashboard/DashboardSidebar.tsx` — audit-log nav link (4 lang)
- `e2e/admin-members.spec.ts` — audit-logs API + page smoke tests
- `messages/az.json`, `en.json`, `ru.json`, `tr.json` — dashboard.auditLog namespace

## Action Types
- `member.created` — green badge
- `member.role_changed` — gold badge
- `member.deleted` — red badge (future TASK-0140)
- `member.password_reset` — blue badge (future)
