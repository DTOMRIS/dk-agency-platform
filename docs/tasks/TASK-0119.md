# TASK-0119 — useLocale + global contrast + DeepSeek contract fix

- Status: ✅ Tamamlandı
- Tarix: 31 May 2026
- Scope: Locale sync, input contrast root cause, AI orchestrator contract

## Fixes
1. Header.tsx + Footer.tsx: pathname.split → useLocale() (next-intl context)
2. globals.css: body color #eaeaea was inherited by ALL inputs → global override for input/textarea/select color:#0f172a bg:#fff
3. ListingForm.tsx AI: targetAgent:'almila' → taskType:'AlmilaCloser', query → userPrompt, structured error messages
