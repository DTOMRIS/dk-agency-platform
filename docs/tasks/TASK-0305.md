# TASK-0305 - News detail schema compatibility hotfix

- ID: `TASK-0305`
- Scope: Restore all public news detail pages that return HTTP 500 when the production database does not yet contain the optional related-toolkit columns.
- Root cause: `getNewsArticleBySlug` selected `related_toolkits` and `related_blog_slug` directly, but the production Neon table did not contain those columns. Render guards cannot catch a database SELECT failure.
- Affected files:
  - `lib/repositories/newsRepository.ts`
  - `drizzle/0017_add_news_related_columns.sql`
  - `app/favicon.ico/route.ts`
- Done criteria:
  - Detail query works before and after the optional-column migration.
  - Related toolkit data defaults to an empty list when columns are absent.
  - Idempotent SQL exists for the missing columns.
  - `/favicon.ico` no longer returns 404.
  - Live-shaped DB query, lint, build, route smoke, and dk-validator pass.
- Rollback: Revert the TASK-0305 commit. The migration is additive and does not require rollback.
- Owner: Codex
- Date: 2026-06-12

## Verification

- Production before deploy: affected Subway detail returns HTTP `500`.
- Production-shaped Neon schema: `related_toolkits` and `related_blog_slug` absent.
- Corrected repository query against the same DB: affected approved article loads successfully with `relatedToolkits: []`.
- Local Next route with real DB: affected detail returns HTTP `200`.
- Local `/favicon.ico`: HTTP `308` to `/icon.png`.
- Target lint: 0 errors.
- Production build: PASS, 0 errors.
- DK validator: PASS 8/8.
