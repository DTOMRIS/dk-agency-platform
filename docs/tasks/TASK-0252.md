# TASK-0252

- Scope: Manual news editor backend — POST endpoint + repository.createNewsArticle()
- Reason: NewsEditorForm frontend (components/dashboard/NewsEditorForm.tsx,
  app/dashboard/xeberler/yeni route, ShareButtons) mevcuttu fakat form submit
  edildiginde POST /api/news/admin 404 donuyordu — endpoint yoktu ve
  createNewsArticle() repository fonksiyonu yoktu. Form actile chalismiyordu.
- Affected: app/api/news/admin/route.ts (POST + Zod CreateNewsSchema),
  lib/repositories/newsRepository.ts (CreateNewsInput tipi +
  createNewsArticle() fonksiyonu). Schema'ya 12 yeni kolon onceki commitle
  eklendi (is_manset/is_top/is_gundem/news_type/telegram_send/logo_overlay/
  seo_title/seo_description/content_az/ru/en/tr) ve migration SQL Neon'da
  uygulandi.
- DoD: POST 201 doner; Zod en az 1 dilde baslik kontrol eder; slug AZ harf
  donushumu ile uretilir; external_url bos ise synthetic deger
  ("manual:{slug}:{ts}") konur; auth canAccessNewsAdmin ile korunur;
  TypeScript path-alias hatalari yok (false positive disinda).
