# TASK-0100 - P&L Simulator i18n

## Scope

Convert the P&L Simulator from Pattern C hardcoded Azerbaijani copy to Pattern A `next-intl` translations.

## Changed

- `app/[locale]/toolkit/pnl/page.tsx` now uses `useTranslations('toolkit.pnl')`.
- `messages/az.json`, `messages/ru.json`, `messages/en.json`, and `messages/tr.json` include the full `toolkit.pnl` namespace.
- Currency and percent values are formatted with `Intl.NumberFormat`.
- Text inputs parse locale-specific decimal separators for AZ/RU/TR and EN.
- `/toolkit/pnl-simulator` and `/{locale}/toolkit/pnl-simulator` alias the existing P&L page for compatibility.
- Playwright smoke coverage validates locale rendering and number formatting.

## Phase 2 (2026-05-19) — Sub-component migration

- PnlForm.tsx: 23 inline Record<Locale> strings → useTranslations('marketinq.plSimulator')
- PnlResult.tsx: 20 inline Record<Locale> strings → useTranslations('marketinq.plSimulator')
- PLSimulator.tsx: 1 hardcoded "USTA" badge → t('tierBadge')
- 44 keys added to marketinq.plSimulator namespace with pnlForm_ and pnlResult_ prefixes
- locale prop removed from PnlForm/PnlResult

## Verification Notes

- Build passes.
- JSON files parse.
- grep pageCopy/Record<Locale> in PnlForm+PnlResult: 0 hits
- PROTECTED untouched
