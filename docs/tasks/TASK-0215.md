# Task Card

- ID: `TASK-0215`
- Scope: AI SEO (SEO + AEO + GEO) upgrade for blog. Add a reusable JSON-LD module and enrich every blog post's structured data so ChatGPT / Perplexity / Google AI Overviews can extract & cite it. Applies to all blog posts via the shared `[slug]` template.
- Affected files:
  - `lib/seo/structured-data.ts` (new — `articleNode`, `breadcrumbNode`, `organizationNode`, `faqNode`, `jsonLdGraph`, `localeUrl`)
  - `app/[locale]/blog/[slug]/page.tsx` (emit a single `@graph`: BlogPosting + BreadcrumbList + Organization)
  - `docs/CHANGELOG.md`
- Do-not-touch list: all PROTECTED files. (No layout edits.)
- Done criteria: build 0 error; blog detail emits one valid JSON-LD `@graph` with BlogPosting (incl. `publisher.logo`, `mainEntityOfPage`, `articleSection`, `wordCount`, `isAccessibleForFree`), BreadcrumbList, and Organization.
- Rollback steps: `git revert` the commit.
- Owner: Doğan / Claude (CTO task)
- Date: 2026-06-07

## Research basis (2026)
- GEO/AEO: AI engines cite content with complete JSON-LD, clear structure, expert quotes/stats/citations.
- Top schema types: Article, **FAQPage** (highest AEO impact), BreadcrumbList, Organization, Person.
- `publisher.logo` (ImageObject) is required for Article rich results — was missing.

## What changed vs before
- Before: minimal BlogPosting (no publisher.logo, string mainEntityOfPage, no breadcrumb, no standalone Organization).
- After: enriched BlogPosting + BreadcrumbList + Organization in one `@graph`; publisher.logo → /icon-512.png; author.url → /haqqimizda; articleSection + wordCount added.

## Phase 2 (not in this task — proposed)
- FAQPage per article (helper `faqNode` is ready) — needs FAQ content/data per post. Highest AEO lever.
- Homepage Organization + WebSite (+ SearchAction) schema for brand entity.
- Same Article enrichment for news (xeberler/haberler).
- Content GEO: ensure expert quotes/stats/inline citations (blog already uses guru quotes / Doğan notes).
