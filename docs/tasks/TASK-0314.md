# TASK-0314

- ID: `TASK-0314`
- Scope: Branded default Open Graph image for the whole app (`app/opengraph-image.tsx`).
- Reason: TASK-0313 follow-up — homepage and pages without their own cover image had no social-share image. Next.js file-based opengraph-image auto-populates og:image AND twitter:image for the root segment and all descendants; per-article images override it.
- Affected files:
  - `app/opengraph-image.tsx`: NEW — 1200x630 ImageResponse, brand gradient (#1A1A2E→#E94560), DK logo mark + tagline. Concrete hex colors only (Satori does not resolve CSS variables — the existing `app/xeberler/opengraph-image.tsx` uses `var(--dk-*)` which render as unresolved; avoided here).
- Do-not-touch list: existing per-segment opengraph-image files, layout metadata.
- Done criteria: tsc clean; `/opengraph-image` returns a branded PNG post-deploy; imageless pages get this card.
- Rollback steps: delete the single file.
