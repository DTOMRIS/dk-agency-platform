# TASK-0180 — Member Profile V2: Schema + API + Admin Approval

- Status: ✅ Tamamlandı
- Tarix: 31 May 2026
- PR: pending
- Scope: member_profiles schema delta, real API, admin review, B2B panel real integration

## Dəyişikliklər

### Schema (Migration 0009)
- 22 yeni sütun: company identity (logo_url, voen, sector, description), contact (authorized_person, position, contact_phone, whatsapp, website), location (city, district, street_address), social (bio, instagram, linkedin), approval workflow (approval_status, reviewed_by, reviewed_at, rejection_reason), visibility_settings (jsonb), slug, language
- 2 index: approval_status (filtered), slug (unique filtered)

### API
- `GET /api/user/profile` — memberProfile ilə genişləndi
- `PATCH /api/user/profile` — 18 yeni sahə + submitForReview + auto slug
- `GET /api/admin/profiles` — status filter + search
- `PATCH /api/admin/profiles/[id]` — approve/reject + audit log

### UI
- `/b2b-panel/profil` — localStorage mock aradan qaldırıldı, real API
- `/dashboard/profil-onay` — admin review səhifəsi, approve/reject modal

### Approval Flow
draft → submitted → approved (public) / rejected (re-edit)
