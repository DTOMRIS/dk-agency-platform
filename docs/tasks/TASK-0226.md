# Task Card

- ID: `TASK-0226` (P1, in progress)
- Scope: Fatura B2B + launch campaign (PR-4). Step 1: launch-campaign helper + journal-untrack infra. (Banner + b2b route + grace gating to follow.)
- Affected files:
  - `lib/launch-campaign.ts` (new — `isLaunchActive`, `daysUntilLaunchEnd`, `shouldWarnLaunchEnding`, `launchEndLabel`; free until 2026-09-01)
  - `.gitignore` + `scripts/auto-journal.mjs` (untrack `docs/SESSION-JOURNAL.md` — stops recurring parallel-branch merge conflicts)
- Do-not-touch list: PROTECTED files. member-access untouched (gating reads plan only).
- Done criteria (this step): build clean; campaign helper exported; journal no longer staged into commits.
- Owner: Doğan / Claude · Date: 2026-06-07

## Notes
- Grace policy: existing invoices always readable + CSV/PDF export; only NEW upload + OCR + AI become premium after the free period. No data hostage.
- Next: LaunchCampaignBanner (30/14/7/1-day warning) + `/b2b-panel/faturalar` route (built on the #318-secured API).
