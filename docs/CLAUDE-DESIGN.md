# CLAUDE DESIGN

## 2026-06-03 - TASK-0186

AI Franchbook Generator uses the existing franchise card/progress language through `FranchiseQuiz` instead of creating a parallel input UI. The component now has two modes:

- score quiz: readiness and buyer checklist
- wizard: text input flows such as Franchbook

The result view is intentionally utilitarian: warning banner, brand/sector header, accordion sections, editable textareas, save and markdown export. Cards are used only for the tool shell and repeated accordion sections.
