// Stats bar hidden until real, verified metrics are available.
// Previous content showed unverified vanity numbers (e.g. "6.7M impressions",
// "99.9% availability") which read as mock data on launch. Restore with real
// values wired from analytics when ready.
export default function StatsBarSection() {
  return null;
}
